---
id: procedure-<slug>
type: procedure
subtype: workstyle
status: active
classification: internal
created: "{{date}}"
updated: "{{date}}"
summary: ""
---

# My workstyle: {{title}}

<!-- Drafted by the AI during onboarding stage 4 (observe first, confirm
     second) and owned by the user from that moment: edit it anytime; delete
     it to restore default behavior exactly. The real file lives at
     `80 User/workstyle.md` with id `procedure-workstyle` and a one-line
     summary of how this user works. Sections that don't apply are removed,
     not left empty. -->

## My words → system concepts
| I say | The system means |
|---|---|
|  |  |

## Speak my language
When filing or reporting, use MY terms first ("filed to your ___"), with
standard metadata doing the real work underneath.

## My tags (apply mine verbatim; translate to fields underneath)
| My tag | Also means |
|---|---|
|  |  |

## My rituals (call them by my name; never restructure them)
-

## My machinery (binding — never rename or move anything these depend on)
<!-- Templates, queries, scripts, saved searches — and the exact paths,
     names, or metadata keys each one needs. -->
-

## Keep (binding — never propose changing these)
<!-- Only the user can put an item here; scan findings stay proposals until
     the user confirms them. Proposing a change to a keep item is a defect. -->
-

## Coexistence posture
<!-- gradual (default) | side-by-side | full-adoption-later.
     Whatever it says: migration of the old corpus is offered once, during
     onboarding, and never again unprompted. -->
