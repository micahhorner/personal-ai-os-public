---
id: note-delta-register-<slug>
type: note
subtype: finding
status: draft
sources: []
related: []
confidence:
verification: inferred
created: "{{date}}"
updated: "{{date}}"
summary: "Delta register for {{source}}: every finding classified into an outcome family (Knowledge / Action / Governance / Nothing) with its source line, voice, and one-line rationale — the owner-reviewable routing map, produced before any note is built (ingest-and-connect 3t)."
---

# Delta Register — {{source}} ({{date}})

**One row per finding. The register exists before any note does; only rows routed to Knowledge reach build-out. The owner reroutes any row by filling the last column; their ruling supersedes the classification.**

Voice split (from `aios measure`, never hand-counted): `{{measure_output}}`

| # | Finding (the human's claim, in their words) | Source line | Voice | Family | Rationale | Owner ruling |
|---|---|---|---|---|---|---|
| 1 |  |  |  |  |  |  |

**Column values** — *Voice*: human · enhancement · solicited · redirection · third-voice (per ingest-and-connect 3a). *Family*: Knowledge (note new/updated — names which) · Action (names the explicit destination in the user's own method) · Governance (the owner's call — surfaced, never made) · Nothing (the reason is mandatory).

## Routing notes

<!-- Only what a row cannot hold: a dedupe target, an uncertain classification explained, a destination that needs a sentence. A table, not an essay. -->
