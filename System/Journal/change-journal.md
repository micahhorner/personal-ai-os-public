---
id: doc-change-journal
type: system-doc
file_class: canonical
authority: canonical
canonical_for: change-journal
created: 2026-07-10
updated: 2026-07-10
summary: Append-only historical audit record of every medium+ change. CANONICAL HISTORY — never disposable, never regenerated, never rewritten (DEC-016).
---

# Change journal

Append-only. One line per medium+ change: date · actor · action · target IDs · outcome · checkpoint ref. This file is canonical historical state — deleting it destroys the audit trail.

- 2026-07-10 · claude · genericize · vault · product master reset to clean generic seed (DEC-020); the owner's personal instance and its full journal history continue at the separate personal-instance repository
- 2026-07-10 · claude · backfill · maintenance change changes were Git-recorded but not journaled at write time (Change Control §7 gap, acknowledged at owner's challenge) · full detail: repo history through 0a6e912 · instance_role manifest addition pending owner ratification
- 2026-07-10 · claude · ratification · DEC-021 · owner ratified the instance_role tripwire after the fact · governance loop closed
- 2026-07-10 · claude · edit (high tier, owner-approved with impact report) · doc-onboarding-specification + 2 derived + skill-capture-tacit-context · stage 6 redesigned: first-content demonstration, tacit optional · checkpoint tag pre-stage6-redesign
- 2026-07-10 · claude · amendment (owner-approved DEC-022) · remove .claude mirrors, pointer-only adapter · spec v1.0.2 · checkpoint tag pre-mirror-removal
- 2026-07-10 · claude (promotion, owner-delegated) · fix · privacy-respecting generation, universal secret scan, safe scaffold, schema de-dup + guard · proven in test vault (probes + workload regression + 4 durable tests)
- 2026-07-10 · claude (promotion, owner-delegated) · amendment DEC-023 · Runtime Kernel + Task Router; reading order rerouted; full stack under developer_mode · 74% reduction, regressions clean
- 2026-07-10 · claude (promotion, owner-delegated) · amendment DEC-024 · capabilities replace skills; single-file contracts; generated component index; lazy generation
- 2026-07-10 · claude · fix · stale skill-id refs (CI caught it; my pre-push check skipped links — process gap noted) · full suite now green
- 2026-07-10 · claude · repair batch 1 (audit-2 H-02, owner-approved with impact report) · doc-canonical-architecture §31 (spec v1.0.5 amendment, DEC-014 consistency) + agent-onboarding-guide + doc-ai-onboarding-procedure + doc-privacy-boundaries + fixture-07 · onboarding state references corrected to the DEC-014 split model (progress + answers) · proven in test vault first (commit 7c89f88, all checks green) · checkpoint tag pre-audit2-batch1
- 2026-07-10 · claude · repair batch 2 (audit-2 H-03, owner-approved) · script-generate + regression test · generated state built at the hosted floor via the centralized search/read privacy predicate: restricted/prohibited/local-only/private notes leave zero trace (no id, filename, summary, hash, relationship, reverse-view key) · proven in test vault (a3d0529: suite 27 OK, live local-only probe zero-trace) · checkpoint tag pre-audit2-batch2
- 2026-07-10 · claude · repair batch 3 (audit-2 H-04/H-05, owner-approved as revised by DEC-025) · manifest protected_objects expansion + stale entrypoint fix · claude-code profile stripped of owner certifications (starts uncertified; L3-with-gates, read-write per DEC-025) · owner test-results file removed from master · onboarding spec v1.0.1 (stage-1 instance_role confirmation) · arch spec v1.0.6 · proven in test vault (20ba9bd, suite 28 OK) · checkpoint tag pre-audit2-batch3
- 2026-07-10 · claude · repair batch 4 (audit-2 H-07/M-02/M-06, owner-approved plan) · script-checkpoint + script-scaffold + tests · exact-tree rollback with post-restore validation; activation requires existing test paths and verifiably flips status; workflow scaffold emits status: draft · proven in test vault (a18dd58, suite 30 OK) · checkpoint tag pre-audit2-batch4
- 2026-07-10 · claude · repair batch 5 (audit-2 H-01/H-02 residues, M-01, M-03, L-01; owner-approved with impact report — router is protected) · doc-task-router v0.2.0 + 3 router-integrity tests · complete dependency rows, onboarding route loads the spec, developer mode loads the architecture spec, deterministic fallback · proven in test vault (3de4f39, suite 33 OK) · checkpoint tag pre-audit2-batch5-6
- 2026-07-10 · claude · repair batch 6 (audit-2 M-05/L-02, owner-approved) · Human User Manual + fixture-05 + aios.py docstring · journaling unified on kernel lanes (ordinary = quiet); honest CLI header; M-04 broader scanning declined by owner (recorded) · proven in test vault (5b85c22)
- 2026-07-10 · claude · delta-audit closure batch (owner-approved: B-01..B-05 + M-10, DEC-026) · router v0.2.0 routes completed + closure test · activation requires recorded passing result · field-level secrecy promise removed (DEC-026) · uncertified default L2 in shipped profile · arch spec v1.0.7 body reconciliation · Active Decisions register installed + routed · CHANGELOG current · terminology sweep · proven in test vault (32cccce, suite 34 OK) · checkpoint tag pre-delta-closure
- 2026-07-10 · claude · documentation batch (clarity audit, owner-approved) · 3 stale fixes + layered plain-English doc set (Product Overview / Quick Start / How the System Works / Implementation and Admin Guide) + Human User Manual v1.1.0 + recovery symptom rows + learning-path reading order · proven in test vault (975afcf, suite 34 OK) · checkpoint tag pre-doc-batch
- 2026-07-10 · claude · release-gate audit fixes (owner-shared final-gate audit, blockers 3/5/6) · recovery guide git instructions corrected (bare checkout leaves post-checkpoint files — script-tested paths documented instead) · System Map registry line reconciled with DEC-024 · CI matrix now proves the python>=3.9 floor and macOS (was ubuntu/3.11 only) · proven in test vault (ba14f02, suite 34 OK)
- 2026-07-10 · claude · readiness-audit closure (owner-shared direct product audit; H-03..H-07, M-01..M-06) · extension model reconciled to v1 truth (scaffold = capability+workflow only; generator off the dead registry; schema v1.1.0; agents' executable instructions current) · checkpoint --zip human-restorable snapshot posture · onboarding journaling + folder guides + vault profile + extension guide + operations contract + changelog reconciled · drift test bans retired machinery on executable surfaces · proven in test vault (ef635aa, suite 36 OK)
- 2026-07-10 · claude · final-polish batch (Fable comprehensive self-audit, owner-approved) · explicit rollback targets · real supersedes-reciprocity check · consolidates data fixes · purpose field dropped (schema v1.1.0) · fixture-specific test mappings · operations.yaml defaults block (same 15 ops, −66 lines) · --zip maintenance snapshots · kernel/router at 1.0.0 · widened drift net caught 5 more stragglers · README states the v1 platform claim · proven in test vault (a16b6b8, suite 36 OK)
- 2026-07-10 · claude · final-audit bug batch (owner decision DEC-028: v1.0 = supervised light product) · recovery deadlock fixed (rollback --confirm works while kill switch off; docs reordered; START-HERE states the exceptions) · Steward two-phase (audits; ordinary runtime executes) · search + indexes return current knowledge only (--all-states to widen) · hidden ids indistinguishable from absent; excluded-count reporting removed · Governance in the drift net; Change Control/Versioning reconciled · proven in test vault (6c685ac, suite 37 OK)
- 2026-07-10 · claude · pressure-test fixes (P-1, P-2) · a clean-instance 2-week synthetic pressure test found: (P-1) a user's first scaffolded capability failed validation — consolidates wrongly required for new capabilities; (P-2) migrate's failure message claimed "vault left as-is" while changes were applied · both fixed; proven in test vault
- 2026-07-10 · claude · documentation addition (owner-supplied + dark mode) · doc-obsidian-setup-tips wired into Quick Start, Implementation Guide, START-HERE · proven in test vault (b241521)
- 2026-07-10 · claude · RELEASE v1.0.0 (owner-approved sequence) · gates 1-5 pass · certification battery 10/10 (owner-present) · gate 6 waived (DEC-029) · docs owner-approved · manifest + package → 1.0.0 · evidence: System/Tests/results/release-evidence.md
- 2026-07-10 · claude · v1.0.1 (owner-ordered findings batch F-1..F-6) · archive-aware dupes · Obsidian embed resolution · script migrations under runner discipline · foreign-conventions rollup · import + filename docs · 5 regression tests · real-corpus proof: TX 22 errors → green · proven in test vault (3d918b0, suite 42 OK)
- 2026-07-10 · claude · v1.0.2 hotfix · drift test scoped off user content in Inbox/Archive (every lived-in instance failed its suite on legitimate user words) · found on the personal instance's first post-1.0.1 run · proven in test vault
- 2026-07-10 · claude · v1.0.3 hotfix · drift bare-word rule scoped off System/Runtime (instance domain routes; found on live the customized work instance) · proven in test vault
- 2026-07-10 · claude · v1.0.4 (owner-tested in live trial) · Obsidian arrives pre-configured: 3 plugins shipped in-vault, Setup-Tips settings pre-applied, minimal workspace seed (450px dual-pane; zero stale state) · one trust-click first open · docs reframed · proven in test vault (285535e)
- 2026-07-10 · claude · v1.0.5 (owner catch) · README "Getting your copy" reframed: was distribution language ("download or clone"), now licensed framing — copies come from the implementer; ownership of YOUR copy stated stronger; license note plain-language · sole offender confirmed by sweep of all user-facing docs
- 2026-07-10 · claude · v1.0.6 (owner catch) · README license note: aggressive second sentence removed — positive framing only, the license file does the boundary-setting · LICENSE.md skills→capabilities
- 2026-07-11 · claude · v1.1.0 workstyle adaptability (DEC-030 accelerated build) · onboarding spec v1.1.0 stage-4 observe-first block, 80 User/workstyle.md map + template, safe-write/ingest consult lines, router load_if_present (3 routes), fixtures 11+12, doc §11 · proven in Test-Vault (760bf85); reviewed change · validate/links/dupes green, 43 tests OK
- 2026-07-11 · claude · v1.2.0 vault import tool (DEC-031) · aios import command (verbatim+mapped modes, preflight/dry-run/apply pipeline, one-time marker, secret gate, source link baseline, Inbox quarantine terminal rule), import-vault capability, onboarding router route, fixture 13 · reviewed change · proven in Test-Vault (c71b8d9)
- 2026-07-11 · claude · v1.2.1 import proving-run hardening + docs sweep · BOTH real migrations replayed via aios import: TX verbatim FULL PARITY (1,095 byte-identical, TX suite 15/266 identical sets, proof engine no-op, health green) + PV mapped green after recorded dedupe resolutions; 5 engine bugs fixed (registry id-collision, patches: mechanism, writable copies from frozen source, empty-dir mirroring, sentence-boundary summaries) + prefix_parent; docs sweep (START-HERE, Quick Start, Product Overview, User Manual §8, Impl Guide 7b, Glossary ×3, AI Operator Manual, indexes) · 57 tests OK · released tag v1.2.1 after review; both instances synced
- 2026-07-11 · claude · DEC-032 (owner-approved after preflight) · drift test bare-word rule deleted; artifact checks remain; v1.0.2/v1.0.3 carve-outs superseded · rehearsed green in Test-Vault (46e0254) · branch parallel fix change, push awaits owner's "file the changes"
- 2026-07-11 · claude · release v1.3.0 (owner-directed direct merge: "can't you just do it here?") · four PMM-migration branches merged --no-ff into main, full suite + validate + vocab green post-merge · DEC-032/033/034/035 registered · changelog + manifest bumped · tag v1.3.0
- 2026-07-11 · claude · v1.4.0 generated System Reference (DEC-039, owner "go") · high-tier script change on branch parallel feature change: aios generate emits System/Generated/system-reference.md from source (12 components, 15 operations, 8 schemas, 14 vocabularies, folder registry) — factual docs self-update by construction; prose stays derived + Steward-drift-flagged (Phase 2, not built); +1 unit test + rebuilt-set assertion (59 total); manifest 1.3.0→1.4.0 · local gate green (validate/links/dupes/59 tests) · plan: Workshop PLAN-documentation-freshness.md
- 2026-07-11 · claude · v1.5.0 prose drift-flagging (DEC-039 Phase 2, owner "go") · high-tier script change on branch parallel feature change: aios health flags derived docs whose recorded x_reviewed_against source version is behind current (advisory, never auto-rewrites prose); two onboarding derived docs marked as in-sync exemplars; +1 unit test (60 total); manifest 1.4.0 to 1.5.0 · local gate green · Phase 3 (autonomous trigger) folds into the workflow-freshness backlog item
- 2026-07-11 · claude · v1.6.0 doc self-monitoring (DEC-039 Phase 3, owner "do it") + terminology fix · branch parallel feature change: aios health reports generated-docs age and warns when overdue (>10 days) — the self-monitoring half of the freshness loop (true background running still needs an external scheduler); +1 unit test. Design Principle 15 wording aligned skills→capabilities (frozen-doc amendment, DEC-024 rename; principles 1.0.0→1.0.1 w/ amendment note; historical skills mentions unchanged) · manifest 1.5.0→1.6.0 · local gate green
- 2026-07-13 · claude · v1.8.0 Method Kit -> AI OS (owner: "promote it to AI OS") · ADD capability method-kit (active) + vendored generic instrument System/Method-Kit/ + optional onboarding module (Stage 8, all 3 docs) + atlas diagram 38 + fixture/result; DEC-052 · dogfooded end-to-end on a synthetic subject, all 8 criteria pass; validate/generate/health 0 errors · built sandbox-first on parallel feature change -> merged to main
- 2026-07-13 · claude · v1.8.0 router completion (owner: "do a preflight, sandbox test it") · System/Runtime/Task Router.yaml onboarding.run-or-resume load += System/Capabilities/method-kit.md — a route must load what its procedure invokes (audit B-01/B-03); the regression test was failing on main after the v1.8.0 merge · PROTECTED OBJECT (System/Runtime/) edited under explicit owner instruction; preflight (4 router invariants, no inverse unused-load test, rollback = 1 line) → sandbox branch parallel fix change → 72/72 tests + certify green → merge to main
- 2026-07-13 · claude · v1.9.0 canonical core doctrine (owner: full preflight + plan approved, "approve and go") · ADD Principle 26 (Design Principles 1.0.1→1.0.2) + Architecture Spec §17 doctrine (1.0.7→1.0.8) + Runtime Kernel §2 orientation line (1.0.0→1.0.1); explicit promotion gate in ingest-and-connect (0.2.0→0.3.0) + safe-write cross-ref (0.2.0→0.2.1); aios health `unverified_in_knowledge` advisory signal (+1 test); doctrine taught in Human User Manual / Product Overview / Human Onboarding Guide + runtime pointer refs; fixture 14-promotion-gate (L3, Certification Battery); DEC-058 · PROTECTED OBJECTS (System/Architecture, System/Runtime, Design Principles) edited under explicit owner instruction · sandbox-first, REBASED onto current live after catching a shared-checkout drift (live had advanced 3 commits; my 17 files disjoint), 74 tests + validate/doc-check/certify green · manifest 1.8.0→1.9.0 · product master only, owner syncs the 3 instances by hand
- 2026-07-14 · claude · v1.10.0 (owner-ordered onboarding audit → build) · backup made a first-class onboarding step · new doc Backup and Recovery Options (full menu + tradeoffs, show-all-options-let-user-choose) · stage-8 backup beat across all 3 parity docs (spec→1.2.0, both derived re-tagged) · START-HERE + Human User Manual links · built in an ISOLATED worktree (collision-safe: a QA session was live on the AI OS) · gates green (validate/links/doc-check/certify/unit); change opened, merge HELD for owner coordination
- 2026-07-14 · claude · v1.11.0 checkup on demand (owner: "build the checkup-on-demand feature", pull-only) · ADD command `aios checkup` (cmd/checkup.py) — one-line readout of inbox/review-due/unverified-in-knowledge (Principle 26 signals), "tidy" when clear; pull-only, no nagging, deterministic/agnostic; +1 test; discoverable via Runtime Kernel §4 + Human User Manual §6; DEC-059 · sandbox-first, gate green · manifest 1.10.0->1.11.0
- 2026-07-14 · claude · v1.11.1 doc currency (owner: "freshen the older manuals") · Product Overview + Human User Manual gain Method Kit; Extension Guide states the doc-check activation gate (DEC-051); How the System Works lists `aios checkup`; 4 docs declare x_reviewed_against: doc-canonical-architecture:1.0.8 so health.derived_doc_drift flags them on the next spec bump · docs-only, no migration · manifest 1.11.0->1.11.1
- 2026-07-14 · claude · v1.12.0 command-documentation gate (owner: "build a test version, run QA, then continue carefully, check for collisions") · ADD command `aios doc-commands` (cmd/doccommands.py) — reads the CLI command list from aios.py via AST, fails if any command is undocumented in Documentation/ or Onboarding/ (aios <cmd> or backticked name; short words not matched in prose); folded into aios health; skips gracefully when no aios.py present; +6 tests (84 total) · sandbox-first (built + QA'd on a scratch copy), promoted to live after a collision check (live clean on main at v1.11.1; checkup already present; the concurrent v1.11.1 freshness work is complementary, no file overlap) · validate/doc-check/doc-commands/health/certify green · manifest 1.11.1->1.12.0 · product master only, owner syncs the 3 instances by hand

- 2026-07-16 · migrate/system · DEC-064: doc-audit documentation-truth gate (composed into health, CI runs health) + ten doc-truth fixes (spec→1.0.10, four derived docs re-stamped) + rollback dirty-tree preservation (audit M-02 closed) + import kill-switch test (H-05 closed) · system 1.15.0→1.16.0 · branch dec-064-doc-audit · rollback: Snapshots/2026-07-16-004247-pre-DEC-064.zip

- 2026-07-16 · system · v1.17.0 (DEC-065): import checkpoint-or-halt (F2 closed) · checkpoint --zip-only (F29) · Local Only folders honored fail-closed (F30) · derived-doc stamp warning (D2 seed) · +7 tests (109) · checkpoint: Snapshots/2026-07-16-044302-pre-v1.17.0.zip

- 2026-07-16 · system · v1.18.0 (DEC-066): privacy fails closed (typos exclude, never expose) · governance fields validated on every record · doc-truth gate resurrected (latest version, not first heading) · search discloses truncation · failed imports allow retry · fixture 04 can now fail · +7 tests (116)

- 2026-07-16 · system · v1.19.0 (DEC-067): Packs/ excluded from canon scanner — every well-formed pack now installs green (F10 closed) · DEC-004 folder indirection made real for renameable folders (rel_folder helper; 3 hardcodes resolved) + docs corrected · verified pack v2.0.0 installs clean with components indexed

- 2026-07-16 · docs · D2 stamping pass: fixed AI Operator Manual §4 (6→9 capabilities, real drift) then stamped 7 verified derived docs with x_reviewed_against (AI Op Manual, System Map, Active Decisions, Operations Guide, Migrations README, Fixture Runner, cert-procedure); 21→14 unstamped remaining

- 2026-07-16 · system/CRITICAL · v1.20.0 (DEC-068): THE CONSTITUTION — spec v1.0.10 (4,603 w) replaced by the verified manual (23,695 w) as doc-canonical-architecture, option C (same id/path/domain — all inbound edges survive) · 7 derived docs re-stamped 2.0.0 · v1 amendment log + v1→v2 concordance preserved inside · 2 live §-refs updated, historical ones kept · doc-audit hardened (§-number + declaration exemptions) after it proofread its own constitution · owner present and approving · rollback: Snapshots/2026-07-16-053227-pre-crowning-DEC-068.zip

- 2026-07-16 · system · v1.21.0 (DEC-069): prose-freshness ENFORCED — unstamped derived doc = validate error; stamp-behind-source = health error (both were warnings); enabled after stamping the last 14 derived docs (each read vs source, not rubber-stamped); proven live (constitution bump blocks build until doc reconciled) · +tests updated · closes the D2 enforcement half

- 2026-07-16 · system/CRITICAL · v1.22.0 (DEC-070/071): AGENT SKILLS PACKAGING — 9 capabilities git-mv'd to System/Capabilities/<name>/SKILL.md (+description field, contracts unchanged) · aios generate emits spec-valid projections to .claude/skills/aios-*/ (all 9 pass skills-ref; validator evidence forced the design: extra top-level keys = error, metadata mangles lists → canonical stays superset, projection is generated; supersedes spec §8's "nest under metadata" + Phase B symlinks — owner approved amendment in chat) · Task Router 13 load paths updated (protected object; approved A3 = the explicit instruction) · doc-check/doc-audit/generate/scaffold globs updated + scaffold births directory form · SKILL.md removed from retired-artifact list · constitution 2.0.0→2.1.0 (§5, §7.1 eighth artifact, amendment logged) · 19 derived docs re-screened vs delta + re-stamped 2.1.0 · Claude Adapter Guide documents the projection surface · +4 emission tests (122 total) · full suite green · sandbox-proven in isolated worktree before branch · rollback: git branch point dcd4f37 (v1.21.0)

- 2026-07-16 · system · v1.23.0 (DEC-072): DOC-PATHS GATE — new `aios doc-paths` (20th command): backticked System/ paths in prose must resolve; the class `links` exempts by design (how the v1.22.0 Migrations-README escape survived green CI) · composed into health → CI-enforced per merge · noise discipline: committed roots only, placeholders/runtime-dirs/history exempt, Migrations README deliberately NOT exempt (planted-bug probe caught docaudit's HIST over-exemption during build) · bonus catch: doc-audit's command matcher skipped backticked names ("19 `aios` subcommands" went unflagged) — fixed, then it caught the Glossary drift as designed · constitution 2.1.0→2.2.0 (tables/censuses re-extracted; test + error censuses had drifted since 2.0.0) · 19 derived docs re-stamped · +6 tests (128) · docs: Glossary, How the System Works, AI Operator Manual, Maintenance Guide, Automations Reference · owner-approved ("do it carefully") · rollback: branch point d88e599

- 2026-07-16 · system/CRITICAL · v1.24.0 (DEC-073/074): AIOS UPGRADE — the product-update importer ships (reviewed change, owner-merged, CI green) · new `aios upgrade <release>` applies a release's System/ (and only System/) into an instance: three-way classify vs the shipped System/.baseline-manifest.json (DEC-074; --make-baseline on master, freshness CI-bound) · pristine→replace, instance-edited→keep+report, both-sides→CONFLICT never merged, router = per-row review list (DEC-073) · no/mismatched baseline → fallback: every diff a conflict, nothing replaced · migrations delivered+listed, never auto-run · version bump earned (gates green + no protected conflict + no pending migration + real baseline; fallback-bump lie caught by its own test pre-merge) · runtime user-content integrity check (content dirs+80 User+.obsidian+Packs fingerprinted pre/post; ported from parallel parallel feature change build, consolidated) · kill switch + checkpoint-or-halt + --dry-run writes nothing · REHEARSED pre-merge on a disposable personal-instance clone: 845 user files byte-identical, version bump correctly refused (evidence retained outside the distribution; findings: instance version labels drift — manifest said 1.5.0, 94/155 files at v1.21.0 — and DEC-069 reddens user notes on upgrade, U1 owner-gated in a separate session) · docs: Upgrading.md new, User Manual §10, Impl Guide, Maintenance Guide, Versioning §4a (1.1.0), Migrations README re-stamped, constitution censuses (7 write cmds, 22 flags, 21 subcommands) · +12 tests (140 total), certify green · rollback: branch point 3df9450 (v1.23.0)
- 2026-07-16 · system · v1.24.1: FRONTMATTER HYGIENE — Diagrams/PIPELINE.md + CATALOG.md gain standard frontmatter (were the only hand-authored System docs without any); generate.py header helper stamps `type: system-doc` on generated markdown docs · found during OKF v0.1 conformance verification for the launch article (private construction record) · validate warnings 7→5 (remaining = repo furniture, deliberate); fixtures untouched by design (raw-input proof) · full suite green (128 tests, all doc gates) · owner-approved in chat ("4 ok, and update documentation, if necessary") · rollback: branch point 7736863 (v1.24.0)
- 2026-07-16 · system · v1.23.1: FRONTMATTER HYGIENE — Diagrams/PIPELINE.md + CATALOG.md gain standard frontmatter (were the only hand-authored System docs without any); generate.py header helper stamps `type: system-doc` on generated markdown docs · found during OKF v0.1 conformance verification for the launch article (private construction record) · validate warnings 7→5 (remaining = repo furniture, deliberate); fixtures untouched by design (raw-input proof) · full suite green (128 tests, all doc gates) · owner-approved in chat ("4 ok, and update documentation, if necessary") · rollback: branch point 3df9450 (v1.23.0)

- 2026-07-16 · system · v1.25.0: OKF-CHECK — new `aios okf-check` (21st command) verifies any tree against Google's Open Knowledge Format v0.1 (frontmatter + non-empty `type` on every non-reserved .md; reserved index.md/log.md exempt) · default target = the manifest's content directories: the knowledge layer IS an OKF bundle (spec's bundle-as-subdirectory form) — the launch article's compatibility claim, machine-checked · read-only, nonzero exit on violation (export/CI gate) · regression test pins the seed knowledge layer CONFORMANT · +9 tests (137 total) · docs: Glossary (21→22), AI Operator Manual, How the System Works · full suite + all gates green · owner-approved in chat ("5 ok, build carefully, update documentation") · rollback: branch point 62e42b3 (v1.24.1)

- 2026-07-17 · system · v1.25.1 (DEC-075): OKF STORY REACHES THE BUYER-FACING DOCS — v1.25.0 shipped okf-check but only the reference docs (Glossary, AI Operator Manual, How the System Works) carried it: enough for the doc-commands gate, absent from every page a buyer reads · added to README (open-standard claim + verify command), START-HERE (one-minute model), Product Overview (why-it's-different line), Human User Manual §10 (new section: check your own portability), Backup and Recovery Options (third risk: a backup in a format you can't read later) · **DEC-075 recorded** — the claim posture (knowledge layer is a conformant bundle via the spec's bundle-as-subdirectory form; never the repo, whose fixtures are deliberately raw; never imply priority; be the superset per DEC-070) — written one release late, noted as such in the entry · **two front-door defects fixed, both gate-invisible**: README claimed "6 AI playbooks" (real: 9 — doc-audit tracks "capabilities", not "playbooks") and "Current status: 1.0.0-rc.1" (25 releases stale — the version gate only matches `system_version: X.Y.Z` phrasing); status line now points at the manifest instead of restating a rotting number · same bug class as DEC-072 (a true claim in a phrasing no gate watches) · **DEBT DEFERRED, recorded in DEC-075**: constitution CLI table + module census lack okf-check (census reads 19 vs real 21) — amending re-stamps 19 derived docs under DEC-069 and those reviews must be genuine; earns its own change · full suite green (137 tests, validate/links/dupes/doc-check/doc-commands/doc-audit/doc-paths/okf-check/health) · owner-instructed in chat ("This needs to be added to our product documentation, positioning, marketing and sales materials") · rollback: branch point db83afa (v1.25.0)

- 2026-07-17 · AI (Claude, Phase C session) · system · okf-export + okf-import shipped (DEC-076 — renumbered from 075 at rebase, claimed by v1.25.1's ruling; fifth estate numbering collision): OKF v0.1 emit/consume surface, export privacy fail-closed + okf-check self-gate, import = immutable draft sources via ingest-and-connect; round-trip proof recorded (results/okf-round-trip.md); ingest-and-connect 0.9.0→0.10.0; +27 tests (176) · validate/links/dupes/doc gates green · v1.26.0

- 2026-07-17 · AI (Claude, Opus 4.8, Phase E session) · system/HIGH · v1.27.0 (DEC-077): `aios measure` — the transcript job (PLAN-standards-execution Phase E, spec A5). New READ-ONLY command: per-voice word/substantive-turn split, `source_note_line` for ingest step 3a, term-origin table (which voice coined a phrase first — the 11-of-18 check), `--extract` (human's turns in isolation, quoted material stripped). Closes the last hand-performed step in `ingest-and-connect` (0.10.0→0.11.0 now CALLS it; Kernel §4). Third-voice trap handled: blockquoted pastes credited to no speaker; vault's own annotation block above the verbatim divider excluded from counts AND term-origin (else a coinage looks pre-existing in the note written to describe it). **Privacy ruling DEC-077 (owner-approved in chat, "that's fine"):** measurement to stdout, content never (2 tests) — a hosted runtime may see the STATISTICS of a `Local Only` source (DEC-065) it may not read; `--extract` writes content for human/local runtime only, kill-switch gated. +20 tests (196 total); fixtures all synthetic (generic-master). Validated against a private historical evaluation corpus (not shipped): reproduces the FINDING (assistant ~6:1; 6 of 7 found titles its coinages, one sampled term the human's) but corrects the by-eye FIGURES (15% vs 11%; the hand pass mis-attributed blockquotes + used an unwritten "substantive" threshold). Docs: Glossary 24→25 commands, How the System Works, Maintenance & Recovery, Constitution §6.2 (fourteen read commands) + §6.4 (twenty-four flags). baseline-manifest regenerated for 1.27.0; generated state rebuilt. **REBASE NOTE:** built first on a 5-release-stale local main (v1.21.0), rebuilt clean on mainline (v1.26.0) after `git fetch` — DEC-077 verified free against the LIVE register, not local (would have collided at DEC-070 otherwise). Full suite green: validate/links/dupes/doc-check/doc-commands/doc-audit/doc-paths/okf-check/health/certify. rollback: branch point d56f4a8 (v1.26.0).
- 2026-07-18 · AI (Claude, Opus 4.8) · correction · The entry above was written pre-final-renumber and names **v1.27.0 (DEC-077)** with rollback point d56f4a8; the change actually shipped as **v1.29.0 / DEC-079** (reviewed change, merged 2026-07-17). It was rebased onto a moved main where DEC-077 (manage-packs rollback) and DEC-078 (obsidian-bases) had landed ahead of it, so both the version and the decision number were reassigned — verified free against the live register at rebase. CHANGELOG, Active Decisions, and the manifest carry the correct v1.29.0/DEC-079; this line reconciles the log with them. Historical wording above left intact (append-only).

- 2026-07-24 · AI (Claude, Opus 4.8, Session 9 docs sweep) · docs · PROSE-PROPAGATION SWEEP for the landed launch stack (v1.22–1.29) — the human-judgment layer the automated gates can't see (stale narrative, not counts). Product docs: How the System Works (§8 generate now emits `.claude/skills/` Agent Skill projections + upgrade added to the machine list), Glossary (Capability def = 9 core + SKILL.md/Agent Skills packaging + generate projection; Generated def notes skill projections), Add-Ons and Packs (obsidian-bases named as the bundled first third-party pack), Backup and Recovery Options (cross-refs `aios upgrade`/Upgrading.md — "wherever backup is taught, the update path belongs"), Diagrams/CATALOG (completeness claim qualified: upgrade/OKF round-trip/measure covered by CLI-surface diagram 22, dedicated flows a tracked follow-up), AI Operator Manual (§3 script list gains `measure`). Onboarding trio re-synced to spec v1.4.0: Onboarding Specification (Stage 8 "Staying current" upgrade-awareness beat + default-pack acknowledgment; Stage 4 OKF/import migration path named; embedded spec_version stamp 1.2.0→1.4.0), AI Onboarding Procedure + Human Onboarding Guide (matching beats, x_reviewed_against→1.4.0). DEC-069 freshness cascade: Quick Start (1.1.1) + Implementation and Admin Guide (1.1.1) re-read against spec v1.4.0 (both already told the upgrade story — re-stamp, no content change). field-feedback (DEC-080, canceled) confirmed absent from every product/onboarding/buyer doc — nothing to retract. Marketing/sales left for the owner voice pass (voice-gated) — captured in a separate Workshop brief. All gates green: validate (0 err/5 pre-existing warns) · doc-check · doc-commands · doc-audit · health. rollback: branch the session-9 documentation change off d94acc1 (v1.29.0)

- 2026-07-24 · system · v1.30.0 (DEC-081): KNOWLEDGE FRESHNESS — ripple triage (aios ripple: changed note → connected neighbors pending until each effect is judged; ack binds to body hash; makes-stale = status only, prose never auto-rewritten), review clocks (freshness.yaml horizons, stamped at promotion, review-due --set-missing backstop; sources/decisions/outputs exempt), workflow cadence (cadence_days → aios workflow last-run/overdue; graduates idea-workflow-freshness, TX parity 5/5 in Workshop RECORD) · never retroactive: master ships enforced_since null + generate skips stamping on generic-master (defect certify itself caught); instance's first generate stamps its epoch + baselines silently · state = System/Logs ledger (regenerable) + event log (append-only); health rolled-up warnings only (owner D5, error flip later release) · constitution 2.3.0→2.3.1 (§6.9) + derived docs re-stamped · capability SKILL.md wiring: maintain-vault / ingest-and-connect / retrieve-context · fixture 17 → L3 battery · +19 tests · built 2026-07-16 vs v1.21.0, rebased 2026-07-24 over nine intervening releases (squash preserved at an isolated backup point) per PLAN-knowledge-freshness (owner-approved) · rollback: commit 1a6bc3d (v1.29.0)

- 2026-07-24 · system · v1.30.1 (DEC-082): DEC-069 SCOPED TO SYSTEM/ — unstamped authority: derived under System/ stays a hard error; on user content it becomes one rolled-up advisory warning (closes U1, upgrade-rehearsal finding 2: lived-in clone went 0→14 errors on the owner's own notes; owner direction 2026-07-16 = freshness yes, approval-gated) · regression test flipped: user-path probe asserts NO error · baseline regenerated · blocks-then-unblocks the v1.30.x instance syncs (found while preparing them)

- 2026-07-24 · system · v1.30.2 (DEC-083): DOC GATES SCOPED — doc-check/doc-audit/doc-paths stay hard errors on the generic master (CI unchanged), become warnings on personal instances (29 residual errors on the first live v1.30.1 upgrade, all gates-vs-sanctioned-customization; fifth lived-in-instance test-scope defect) · no-manifest scaffolds stay strict · +3 tests · baseline regenerated

- 2026-07-24 · docs · v1.30.3: FRESHNESS AT THE FRONT DOOR — onboarding spec 1.5.0 (stage-8 awareness beat) + Human Guide/AI Procedure in voice + Upgrading.md 1.1.0 (per-vault epoch activation) · 4 downstream stamps refreshed after delta review · gap found by owner closeout question

- 2026-07-24 · system · v1.30.4 (DEC-084): DUPES CONVENTION EXEMPTION — per-project convention basenames (HANDOFF.md; user-extensible via 80 User/dupes-conventions.yaml) warn instead of error when ids and folders are distinct; same-folder/duplicate-id stays hard · live case: the personal instance's Method-required HANDOFFs · +3 tests · baseline regenerated
- 2026-07-24 · claude · v1.30.5 docs-only (owner: "go" on the propagation chip) · DEC-063 voice attribution + knowledge-note standard propagated to the human-facing docs — Human User Manual §1/§8, AI Operator Manual §6/§7, How the System Works §6/§9, Product Overview differentiator, Glossary +2 terms, onboarding stage 6 across the parity trio (spec 1.5.0→1.6.0, both derived re-tagged 1.6.0) · fixture 16 added to ingest-and-connect `tests:` (was certifying 3a/3b uncited) · built in an ISOLATED worktree (collision-safe; shared checkout in use by parallel sessions) · manifest 1.30.4→1.30.5
- 2026-07-25 · claude · v1.31.0 (DEC-085, owner-approved spec SPEC-session-handoff, all four D-recommendations accepted): SESSION HANDOFF — tenth capability `session-handoff` (System/Capabilities/session-handoff/SKILL.md) + router routes `close-session`/`resume-session` (protected objects; spec approval = the explicit instruction) · close = journal sweep → checkpoint → validate → honest resumable stopping point; resume = record-first, claims re-verified against live state; substantive sessions only · form personalized via new `80 User/handoff.md` pointer (DEC-036 pattern; GENERATOR 0.2.0→0.3.0 emits it) or method pack artifact · fixture 18-session-handoff joins L3 battery · diagram 40 (Session Handoff Loop) + coverage/metadata/CATALOG · constitution 2.3.1→2.4.0 (§5.15, §2.6 routes 14→16, census 14→15, §9.2 regularized — missing 17-knowledge-freshness row was live drift) · Glossary/HtSW/manuals/80 User _ABOUT updated · 18 derived docs re-screened + re-stamped 2.4.0 · baseline regenerated (255 files) · suite 223 OK, validate/health/certify 0 errors · built in an ISOLATED worktree from mainline (shared checkout dirty + behind)

- 2026-07-17 · system · v1.31.1 (DEC-086): CONSTITUTION COMMAND CENSUS RECONCILED — §6 tables 5 releases behind source (OKF: okf-check DEC-075, okf-export/okf-import DEC-076; freshness: ripple/workflow) reached CLI+Glossary+How-the-System-Works but never the constitution's §6.2/§6.3 tables or amendment log · corrected §6.2 read 14→18, §6.3 write 7→9, §6 intro 20→27 cmds / 19→26 modules, 3 okf-* name-mappings · amendment v2.4.1, constitution 2.4.0→2.4.1 · 19 derived docs re-stamped 2.4.0→2.4.1 (confirm-unaffected/already-consistent — derived were AHEAD of constitution) · closes DEC-075 debt · **DEC-085 collision (sixth): session-handoff took 085, mine renumbered to 086 — caught by checking origin at rebase** (branch had diverged; reset to mainline + re-applied) · FLAGGED NOT FIXED: runtime-errors census "129 messages across 19 modules" (modules now 26) · full suite + certify green · docs-only bump 1.31.0→1.31.1 · owner-instructed "fix carefully" · rollback: branch point fab7f50 (v1.31.0)

- 2026-07-17 · system · v1.32.0 (DEC-087): aios docgen — self-generating <!-- docgen:KEY --> blocks; --check in health fails a stale block; freshness trilogy complete; rebased 3× onto fast-moving main (1.31.0/DEC-084 → 1.31.1/DEC-086 collisions → landed at 1.32.0/DEC-087)
- 2026-07-25 · claude · v1.33.0 determinism fixes (owner-approved audit routing, DEC-088) · retrieve-context mandates aios search/read (0.4.0) · vocab folded into health composite · NEW aios ripple --refs (rename-time reference sweep) · PROTECTED OBJECT System/Governance/Change Control and Preflight.md edited under explicit owner instruction (tier table points at --refs; 1.0.1) + Impl Guide re-stamped · +5 tests (228 OK) · built in isolated worktree off mainline df5713f; renumbered 086→088 / v1.32→v1.33 and rebased onto 64f245b (v1.32.0) at landing — seventh collision
- 2026-07-25 · claude · v1.34.0 aios resolve (owner-approved audit routing, DEC-089) · NEW cmd/resolve.py: match ladder + supersession-chain walk + all-states dedupe visibility + privacy fail-closed + --mint id/filename twin · safe-write 0.3.0 / ingest 0.13.0 / retrieve-context 0.4.1 now mandate it · +10 tests · same worktree, stacked on v1.31.0 · renumbered 087→089 / v1.33→v1.34 at landing
- 2026-07-25 · claude · v1.35.0 aios pack (owner-approved audit routing, DEC-090) · NEW cmd/pack.py: 7-clause manifest gate, INSTALL/UPDATE plan, checkpointed place/replace, DEC-077 recovery as code, leave-no-trace remove · manage-packs 0.3.0 delegates mechanics, keeps plan+owner-OK · +11 tests incl. forced-failure recovery · stacked on v1.32.0 · renumbered 088→090 / v1.34→v1.35 at landing
- 2026-07-25 · claude · v1.36.0 (DEC-091, owner rulings 2026-07-25: SPEC-backup-onboarding + standards-ask rider, one change — both edit the DEC-062 trio): BACKUP EVIDENCE — health counts local-only content (DEC-065 path segment + ai_use frontmatter) vs `80 User/LAST-BACKUP.txt` marker; new `System/Scripts/backup-clone.sh` writes the marker only after a successful clone; no marker = never, malformed = never, readable = "evidence of that run only" — false "covered" impossible, warning never error · options doc 1.1.0 (+account-independent axis, marker convention); backup story swept into Product Overview / Quick Start ("eight things") / Human User Manual / Admin Guide · STANDARDS ASK — onboarding stage 5 asks voice + authoring (skippable), writes `80 User/voice.md`/`authoring.md` pointer files or names the shipped defaults (closes the DEC-063 nothing-routes rot one layer up) · onboarding spec 1.6.0→1.7.0, Guide + Procedure re-derived 1.7.0, Quick Start + Admin Guide re-read + re-stamped · DEC-062 parity gains a scripted proof (unit test: stamps pinned to spec's current version, eight stages in all three) · +13 tests (236 total) · baseline regenerated · built in an ISOLATED worktree from mainline (v1.31.0) · renumbered 086→091 / v1.32→v1.36 at landing, stacked on parallel feature change
- 2026-07-25 · claude (fable) · v1.37.0 (DEC-092): sync storage boundary — fourth governance field `sync: allowed|local-only` (vocabularies 1.2.0, 7 class schemas 1.1.0) · validate cross-checks root .gitignore both directions (conservative matcher: unverifiable never "covered"; new lib/gitignore.py) + folder/field disagreement error + rolled-up restricted warning · okf-export fails unknown sync closed, local-only never exports · onboarding trio 1.7.0 stage-2 storage question (DEC-062 parity, scripted check) · safe-write 0.4.0 write-time counsel · constitution v2.5.0 (§3.3/§4.3/§4.4) + 19 derived docs re-stamped · Privacy 1.1.0, Metadata Dictionary 1.1.0, Backup Options 1.1.0, Glossary/HTSW/HUM/AIOM propagated · product .gitignore backs Local Only/ by default · fixture 19 + test_sync_boundary.py · additive, no migration · gate suite green on branch parallel feature change · MERGED 2026-07-26 (merge pass 1/6): renumbered v1.34.0→v1.37.0 / DEC-089→DEC-092 (eighth collision — 1.33–1.36 and DEC-088–091 landed first), safe-write 0.3.0→0.4.0 (both branches bumped it), fixture 19 kept, constitution v2.5.0 kept

- 2026-07-25 · claude · v1.38.0 (DEC-093, owner-approved SPEC-ingest-triage, Wave 3 of the 2026-07-25 rulings): INGEST TRIAGE + PROMOTION-GATE CHECKLISTS — ingest-and-connect 0.13.0→0.14.0: new step 3t triages every extracted finding into the four outcome families (Knowledge/Action/Governance/Nothing) via a delta register BEFORE build-out; register canonized as ninth template `delta-register.md` (from the two proven 2026-07-14 prototypes); step 4's dangling "four outcomes" resolved; step-5 per-class verification checklists (refusal names the failing line); step 2 `provider`→`provided_by` bug fixed (+ AI Onboarding Procedure same fix) · 3a/3b doctrine untouched (spec fence) · fixture 25-ingest-triage joins L3 battery · test_ingest_triage.py (16 tests) pins the prose contracts · constitution 2.5.0→2.6.0 (§4.5 nine templates, §5.2 pipeline, §4.9 checklists, §9.2 twenty-one fixtures, amendment v2.6.0) · 19 derived docs re-stamped 2.6.0 · Glossary (+Delta register, +Outcome families), HtSW, both manuals, Certification Battery, diagrams CATALOG updated (diagram 14 SVG flagged pre-triage, refresh tracked) · deferred as own finding: general field-names-vs-schema prose gate (not cheap enough for doc-audit) · baseline regenerated · built in an ISOLATED worktree from mainline (v1.32.0) · MERGED 2026-07-26 (merge pass 2/6): v1.38.0/DEC-093 held as claimed; fixture 19→25 (sync-boundary took 19), constitution amendment 2.5.0→2.6.0, ingest capability 0.13.0→0.14.0 (both branches had bumped it to 0.13.0); ALSO REPAIRED HERE: parallel feature change had dropped the v2.4.1 amendment header, gluing its body onto v2.5.0 — header restored; and §9.2 never listed 19-sync-boundary — added
- 2026-07-25 · claude · v1.39.0 to-do + project routing (DEC-094, owner rulings 2026-07-25: SPEC-todo-project-routing approved) · NEW capabilities manage-todo (item contract what·why·blocks·path enforced with ONE question max; link-check rot detector flags dead paths in place) + start-project (generic floor: resolve-first, folder + schema-valid note, generate registration; plans/handoffs/gates stay method-pack territory, consult-by-mention) · seed To-Do.md ships at root · PROTECTED OBJECT System/Runtime/Task Router.yaml gains 2 normal rows under the spec's explicit owner approval · fixtures 20-todo-routing + 21-start-project · diagrams 41/42 · constitution 2.6.0→2.7.0 (routes 16→18, components 15→17) + derived docs re-stamped · DELIBERATE DEFERRALS: onboarding-trio To-Do mention (trio touched by pending branches; mention lands in Quick Start/manuals) + instance delivery of root To-Do.md (rides the queued upgrade root-files fix) · built in an ISOLATED worktree off mainline fc30a87 (v1.36.0) · MERGED 2026-07-26 (merge pass 3/6): v1.39.0/DEC-094 held as claimed; only the constitution amendment renumbered (2.5.0→2.7.0, 19 derived docs re-stamped); fixture census now twenty-three
- 2026-07-25 · claude · v1.40.0 the routed decisions space (DEC-095, owner rulings 2026-07-25: SPEC-decisions-space approved, D1-D3 recommendations ratified) · NEW top-level Decisions/ folder (D1: as findable as the Inbox; registry id `decisions`, manifest content dir, decision.yaml directories updated with 20 Knowledge tolerated — no retroactive migration) + capability record-decision (thirteenth; honest bar: closes alternatives or changes scope, ONE question max, supersede never delete) + generated register view index-decision.md (D3: newest first, by project, numbering DERIVED never claimed — parallel creates cannot collide, race simulated in test_decisions.py) · D2: one space, `project:` field (schema 1.1.0) link-checked + reverse-view inbound decisions line · PROTECTED OBJECT Task Router gains 1 normal row under the spec's explicit owner approval · seed ships folder + _ABOUT + one example decision · fixture 22-decisions-space joins L3 battery · diagram 43 · constitution 2.7.0→2.8.0 (routes 18→19, components 17→18, folders 12→13, fields 52→53) + derived docs re-stamped · Active Decisions cross-referenced only (user decisions never land there) · DELIBERATE DEFERRALS mirror v1.39.0: onboarding-trio mention (trio touched by pending branches) + instance delivery of the new folder (rides the queued upgrade root-files fix); DoD-4 dogfood is instance work · built in an ISOLATED worktree off parallel change line 7150ac2 (v1.39.0) — STACKED, merges after it · MERGED 2026-07-26 (merge pass 4/6): v1.40.0/DEC-095 held as claimed; constitution amendment renumbered 2.6.0→2.8.0 · MERGED 2026-07-26 (merge pass 4/6): v1.40.0/DEC-095 held as claimed; constitution amendment renumbered 2.6.0→2.8.0 (19 derived docs re-stamped); §4.3 field census corrected to fifty-three (both this branch and parallel feature change had independently written fifty-two)
- 2026-07-25 · claude · v1.41.0 the working class (DEC-096, owner rulings 2026-07-25: SPEC-working-material approved, option (a) ratified) · NEW System/Schemas/working.yaml — eighth content class for project working material (plans/logs/captures/drafts/scratch), required fields deliberately LIGHT (id/type/subtype/dates/summary/status) so typing costs less than staying untyped · working_subtypes vocabulary + System/Templates/working.md · BOUNDARY ENFORCED BOTH WAYS, a first, asymmetric per DEC-082 (schemas' directories: had been documentation-only — verified empirically: type: note under 10 Projects validated green before this): working outside the project layer (+Inbox/Archive) = ERROR (new class, strict), type: note under the project layer = ONE rolled-up ADVISORY warning naming the retype (amended from hard error before merge on review against DEC-082 — lived-in vaults never go red on the user's own notes; DEC-092 grandfathering shape), project-layer no-frontmatter warning names the class gently (untyped legal, never auto-typed) — validate rules 25→28 · retrieval: working excluded from default aios search (knowledge-first); NEW --project <Name> scope surfaces a project's working material (status gate yields inside explicit project scope); read --id and the Steward (1.1.0) see them · consumers WIRED not implied: start-project 0.2.0 names the class for non-project-note files, retrieve-context 0.4.2 names exclusion + scope, Method Kit generator declares the type on project-layer skeletons · DELIBERATE DEFERRALS: ingest-and-connect Action-family destination line (text lived on then-unmerged parallel feature change — one-line edit still owed post-merge, recorded in DEC-096) + diagram 08 refresh (still draws pre-working classes) · DoD-4 dogfood DONE in scratch vault: real Workshop PLAN copy re-typed frontmatter-only, body hash-verified unchanged, validates green · fixture 23-working-material + test_working.py · constitution 2.8.0→2.9.0 (classes 8→9, vocabularies 15→16, values 72→77, flags 32→33, validate rules 25→28) + derived docs re-stamped · built in an ISOLATED worktree off parallel change line 92234a3 (v1.40.0) — STACKED, merges after todo-routing AND decisions-space · MERGED 2026-07-26 (merge pass 5/6): v1.41.0/DEC-096 held as claimed; constitution amendment renumbered 2.7.0→2.9.0 (20 derived docs re-stamped); vocabulary/value census re-derived from source (16/77 — this branch and sync-boundary had each counted from a 14/70 base); templates census nine→ten; fixture census twenty-four
- 2026-07-26 · claude · v1.42.0 small-fixes bundle (DEC-097, owner rulings 2026-07-25 Wave 5): IMPORT REVIEW QUEUE — stubs carry review_due (+14d default, plan/CLI tunable), generated review-queue.md beside the plan, truthful "Contains:" lines (empty scaffolding named, files-empty projects still stubbed), health `[import-review]` rolled-up finding on the marker sentence, truncation regression seeded · UPGRADE ROOT FILES — CLAUDE.md/pyproject.toml/README.md/START-HERE.md join three-way classification (explicit commented list; LICENSE/CHANGELOG/CONNECT-CHECK/manifest stay user-owned; To-Do.md + Decisions/ seeds additive-when-absent; baseline now records root files) — closes the PMM stale-CLAUDE.md gap · HEALTH RIDERS (audit finding 4, verified absent from merged main) — per-item inbox age >14d `[inbox]` + generated-tamper digests (`System/Generated/.digests.json`) `[generated-tamper]` · OBSIDIAN — seed app.json userIgnoreFilters System/.obsidian/.trash (down-rank, never hide); regression pins upgrade never touches .obsidian · fixture 24-small-fixes · import-vault 0.2.0 · Upgrading.md 1.2.0 · +51 tests (287 OK) · validate/links/dupes/health/certify green · baseline regenerated · built in an ISOLATED worktree off mainline fc30a87 (v1.36.0); MERGED 2026-07-26 (merge pass 6/6, last): v1.42.0/DEC-097/fixture 24 ALL held as claimed — no renumber needed; the Certification Battery L3 row also gained 23 working-material, which its own branch never added; ONE CODE FIX required by the merge: upgrade's content-integrity check now excludes `seeded_root_files` (v1.40.0 made `Decisions/` a content directory, so the DEC-097 additive seed read as user-file drift — a cross-branch interaction invisible on either branch alone)
- 2026-07-26 · claude · v1.43.0 the intake bundle (DEC-098, owner rulings 2026-07-25: SPEC-doc-extraction + SPEC-intake-dedupe approved) · NEW `aios extract` — .docx → Markdown from the STDLIB ONLY (zipfile + xml.etree; python-docx refused, PyYAML stays the sole third-party dep), preserving heading levels, list nesting AND numbering (bug found in build: a bullet sub-list reset its parent's ordered counter), emphasis, hyperlinks, tables, images written beside the original and referenced AT TRUE POSITION with percent-encoded paths · VERIFICATION IS THE FEATURE: every text-bearing paragraph proved present character-for-character (three renderings of the output — raw, markup-stripped, prefix-stripped — because a gate that cries wolf gets switched off), counts in the report on every run, FAILED proof emits NO Markdown at all and files the original + a pointer note naming the failure; no best-effort mode and no flag to add one · detect-and-report with counts for footnotes/endnotes/comments; text-box prose IS extracted (position caveat stated) and de-duplicated across Word's mc:Choice/mc:Fallback double-write; originals immutable in Attachments/ named in original_location; derivative born status: draft; Local Only sources keep original+media INSIDE the fence (DEC-065) · NEW `aios dedupe-batch` — the question `dupes` structurally cannot ask (dupes compares records to CANON by id/title/alias): normalize → hash → pairwise matrix with BOTH sides' percentages + per-file unique contribution; <8-word paragraphs excluded from the matrix and counted separately so boilerplate never reads as corroboration; REPORT NEVER AN ACTION — never discards, never classifies, because the proven near-duplicate was the SUPERSET; --annotate writes x_overlap_* frontmatter AFTER the human decides, bodies byte-identical · REGRESSION RUN ON PRIVATE HISTORICAL CAPTURES (read-only, measurements only — no content read or reported): capture A 97% repeated (3% unique) ✓ matches the prior record, 96% of capture B (record: 95%), 53% of capture C (record: 49% — 4 points apart, the throwaway script normalized differently; NOT tuned to fit, and stable at 96/52-53 across thresholds 1/4/8), capture D 100% unique ✓; fixture 27 reproduces 97/95/49 synthetically at 1:100 because the real corpus is private and out of repo · extract also exercised read-only over 8 REAL .docx (5,269 text-bearing paragraphs, 100% verified, 17 images) — 3 real verification failures found and fixed during the build, all in the CHECK not the converter · WIRED NOT IMPLIED (DEC-063 lesson): ingest-and-connect 0.15.0 step 1 (multi-file → dedupe-batch first) + final line (extractable document ≠ binary) + 3t Action family NOW NAMES ITS DESTINATION (To-Do.md / 10 Projects, DEC-094 — the follow-up v1.41.0 recorded as owed; the row names the ARTIFACT not the procedure, which also keeps the router's B-03 closure check honest rather than bolting two capabilities onto every inbox route), import-vault 0.3.0 step 1a (overlap check before the plan) · fixtures 26-doc-extraction (L2) + 27-intake-dedupe (L3), both built by COMMITTED GENERATOR SCRIPTS the unit suite imports — no opaque binary, no drift between fixture and test · 46 new unit tests (345→391); both writers joined the kill-switch coverage regression in test_commands.py · constitution 2.9.0→2.10.0 + CENSUS RECONCILED FROM SOURCE, not from the previous number: commands 30→32 (§6 intro said 27 — docgen/resolve/pack had reached the CLI but not the tables; §6.2 read 18→22, §6.3 write 9→10), flags 39→40 (§6.4 heading claimed thirty-three over a table of 25 — the 15 absent rows added rather than the heading alone corrected), fixtures 26→28 (§9.2 had never listed 24-small-fixes) · §8.2's index census left untouched: the document declares it a dated snapshot, and half-correcting it would be worse · 20 derived docs re-stamped 2.10.0 (Product Overview, Human User Manual, AI Operator Manual, How the System Works, Implementation Guide, Glossary ×3 entries, Maintenance Guide, Inbox/Sources _ABOUT carry real edits; the rest reviewed and unaffected) · built in an ISOLATED worktree off mainline 56028e8 (v1.42.0) · full gate suite green: validate 0/5 · links 0 · dupes 0 · doc-check 0 · doc-commands 32/32 · doc-audit 0 · doc-paths 0 · health 0/0 · docgen current · certify PASS · baseline regenerated · additive, no migration
- 2026-07-26 · claude (fable) · v1.44.0 the adversary pre-build gate (DEC-099, owner rulings 2026-07-25: SPEC-adversary-gate approved; staged design 2026-07-12, never built) · NEW capability `adversary` (fourteenth) — a capability NOT an agent (ephemeral execution, durable procedure; QA-harness precedent, no new contract/authority) · report in FIVE FIXED SECTIONS (load-bearing assumptions + how we'd know they're false · unnamed failure modes · the cheaper version · scope traps · the plan-killer named singly), every finding cites its line AND states its falsifier, verdict proceed/proceed-with-amendments/redesign, honesty rules (attack the work not the author; name at least one thing right), mandatory closing line (soundness never demand) · WIRED AS A GATE OR IT'S DECORATION: build-extension 0.1.0→0.2.0 step 3b — High-tier builds owe a pass BEFORE anything is created; Low/Medium ungated on purpose; a skip is recorded in the plan as skipped-with-owner-OK, never silent; the verdict advises and never blocks · report filed as `type: working` subtype log (DEC-096's class, first external consumer) beside the plan, never in 20 Knowledge; rejected findings get one line why · PROTECTED OBJECT Task Router gains a `red-team-plan` normal row (BEYOND the spec's stated minimum — deliberate: DEC-094/095 both record that a capability nothing routes to becomes the next forgotten convention, and the fallback would have sent "red-team this" to answer-question) + the developer `_all` load · fixtures 28-adversary-seeded-flaw + 29-adversary-retrodiction join the L3 battery, PERMANENTLY — they are the whole justification for a component over a checklist, and the capability says so about itself · EVALS RUN BY INDEPENDENT SESSIONS (self-authored + self-run proves nothing — the v1.0.0 battery's own lesson): 29 = 3/3 planted defects found (false scheduler assumption, unnamed egress/privacy failure mode, cheaper no-new-component path), gate fired before construction, exactly one file written (the report), verdict redesign; 30 = COLD retrodiction on the original three-job `aios measure` filing, reached the recorded correction (build the transcript job alone) unaided, verdict proceed-with-amendments · THE EVALS EARNED THEIR KEEP TWICE BEFORE SHIPPING: (1) run 1 disclosed that adversary/SKILL.md illustrated its doctrine with fixture 29's own case — and SKILL.md loads on EVERY pass, so the procedure was handing the eval its answer; file now deliberately unillustrated + leak-regression test, (2) run 2 reported the red-team-plan row omitted safe-write which step 8 requires — a breach of the router's own full-dependency-set invariant (audit-2 H-01); row fixed + pinned by test · diagram 44 · test_adversary.py (25 tests, incl. both anti-disarm regressions) · constitution 2.10.0→2.11.0 (components 18→19, capabilities 13→14, routes 19→20, atlas 43→44, fixtures 28/29; §9.2 also picked up 24-small-fixes, which the last merge pass left unlisted) + 20 derived docs re-stamped · ALSO, unrelated: the DEC-062 onboarding trio takes its two DEFERRED mentions now that v1.39.0/v1.40.0 have merged and the parity lock is free — stage 4 names the root To-Do.md (one offer to capture a just-named job; nothing seeded silently), stage 8 gives the decision review's amendments a home in Decisions/ via record-decision and says the product-register boundary out loud (closes a dangling pointer: the review promised "a new dated decision" and never said where); spec 1.7.0→1.8.0, Human Guide + AI Procedure re-derived, Quick Start + Admin Guide re-read and re-stamped, scripted parity proof still green · DELIBERATELY NOT BUILT: the pre-install trust scan (same engine, pointed at a pack) — designed for as a second entry framing, not built here · additive: no schema change, no migration, no new aios command · baseline regenerated (309 files) · built in an ISOLATED worktree off mainline 56028e8 (v1.42.0) · MERGED 2026-07-26 (merge pass 2/2, second): v1.44.0/DEC-099 HELD as claimed; the constitution amendment renumbered 2.10.0→2.11.0 (the intake bundle merged first and took 2.10.0) and 19 derived docs re-stamped; fixtures renumbered 29-30→28-29 (this branch had assumed intake would use 27-28; it actually used 26-27, so 28 was the next free slot and the set is gapless 01-29); §9.2's 24-small-fixes row taken from this branch's wording, the 26/27 rows from intake's; Certification Battery L2/L3 rows unioned across both branches
- 2026-07-26 · claude (fable) · v1.46.0 folder rules move into the procedures that run them; `_ABOUT.md` becomes description-only (DEC-101, owner rulings 2026-07-25: SPEC-about-rules-relocation approved; executes three VERBATIM owner rulings of 2026-07-15) · THE DEFECT: the `_ABOUT.md` signposts in all eight content directories were the ONLY home of three real operating rules (Inbox ~20-item threshold, promote-lessons-before-archiving, folders stay flat) and NOTHING LOADS THEM — not one Task Router row references any of the eight, so a runtime could follow every procedure it was handed and still break all three; proof was live not theoretical (the owner's vault grew three Knowledge subfolders, one created BY the session diagnosing unloaded rules) · principle recorded: A RULE LIVES INSIDE THE PROCEDURE THAT IS RUNNING WHEN THE RULE MATTERS; ANYWHERE ELSE IT IS DECORATION (same shape as DEC-063 user standards + DEC-091 onboarding ask, now closed for the folder layer) · METHOD — SORTING TABLE BEFORE SURGERY: every sentence in all eight files classified description (stays) vs rule (moves) with old-location → new-location mapping per rule, written and reviewed BEFORE any edit (the audit trail that proves nothing was lost in transit): 52 sentences · 17 description · 35 rule, of which 28 were ALREADY stated in a loaded procedure and were deleted as duplicate copies (two copies of a rule become two different rules; they diverge silently because both keep looking correct) and 7 relocated into a procedure that did not carry them; +1 rule entered new (ruling 3's extension to 40 Outputs existed in no file) · SPEC SAID SEVEN FILES, THERE ARE EIGHT — `Decisions/_ABOUT.md` arrived with DEC-095 on 2026-07-25; covered · RULING 1 (verbatim: "Inbox processing should never be automatic. It should be manual and user directed") → ingest-and-connect 0.16.0 (new preamble: no automatic drain, threshold is a PROMPT not a trigger, report count + oldest age, offer, STOP), maintain-vault 0.3.0 step 2 (count/offer/stop; a maintenance pass may surface the Inbox, never empty it, and is not the exception just because the user asked for maintenance), inbox-processing workflow 1.2.0 (NEW §0: cadence_days means REPORTS ITSELF DUE not runs; no scheduled task may drain; a general "tidy up my vault" is explicitly NOT the yes; do not start "just the first few"), weekly-maintenance 1.2.0 step 2, steward 1.2.0 duty 1 · RULING 2 → safe-write NEW section "Archiving (including closing a project)" whose STEP ORDER IS THE RULE: lessons promoted to 20 Knowledge and listed BEFORE anything moves to 90 Archive; working material never graduates as-is; step 3 makes a skipped lessons step a FLAGGED INCOMPLETENESS ("nothing durable to promote" is a finding the human confirms, never an assumption on the way to moving a folder); mirrored in maintain-vault 3 + steward 1a · RULING 3 → safe-write step 4 (the folder-decision step), EXTENDED TO 40 OUTPUTS; affiliation = tags + the project note as index; NO NEW INDEX TYPE (a second index is a second thing to drift); same step now also carries the `derived_from`-on-every-output rule, which had NO procedural home (not required in output.yaml) · ONE LINE OF CODE — validate rule 29: subfolders under 20 Knowledge / 40 Outputs = ONE ROLLED-UP WARNING, NEVER AN ERROR (DEC-082 binds; DEC-092 grandfathering shape), silent once the user declares the folder deliberate with an `_ABOUT.md` of their own (reuses the artifact this release redefines rather than inventing a marker), silent for `Local Only/` (DEC-065), registry-resolved paths (DEC-004), immediate children only · INSTANCE NOTE RECORDED NOT ACTED ON: the owner's three live Knowledge subfolders are HIS to resolve; the product reports and never force-flattens · FINDING SURFACED RATHER THAN IMPROVISED (the spec required this): PROJECT CLOSURE HAS NO SINGLE HOME — no close-project capability, no router row, no workflow; closure happens as a safe-write move, which is where the rule was put; a dedicated closure route stays an owner call · displaced DESCRIPTION rehoused rather than deleted: the 80 User file inventory → Human User Manual §8 (which had previously deferred TO the _ABOUT), pointer discipline + its 2026-07-14 four-days-unread evidence → Onboarding Specification stage 5 (1.8.0→1.9.0; both the spec AND the AI procedure had been CITING the _ABOUT as the source of a rule nothing loaded), trio parity held (guide + procedure 1.9.0, DEC-062) · SWEEP FOR AUTO-DRAIN PROMISES ELSEWHERE (ruling 1 required it): 11 passages fixed across How the System Works ×2, Human User Manual ×3, Flows/7-Weekly-Maintenance ×3, Flows of Work, Diagrams CATALOG, and constitution §5 step 2 ("Inbox → zero" was still shipped doctrine) · fixture 30-folder-rules (L3; three probes — 21 seeded Inbox items draw a prompt and NO drain until an explicit yes, closure names the lessons step and a skip is flagged, the flat folders hold by persuasion; committed seeder script, 21 not 20 so the fixture tests the behaviour not the boundary) + test_flat_folders.py 12 unit tests (416→428) · CENSUS RECONCILED FROM SOURCE not from the previous number: constitution §6.5 heading read TWENTY-EIGHT validate rules over a 28-row table against a validate.py carrying 37 live rules — DEC-092's sync-boundary rules and DEC-066's universal governance check had shipped without ever reaching the table; recounted to THIRTY-EIGHT and the ten missing rows added rather than the heading alone corrected (the v2.10.0 precedent for commands/flags); §9.2 fixtures 30→31 · constitution 2.11.0→2.12.0 + 19 derived docs re-stamped · doc-audit caught 4 false number-claims from my own new prose ("eight folder `_ABOUT.md` files" reads as a folder-count claim) — reworded, gate working as designed · baseline regenerated (321 files) · full gate suite green: validate 0/5 (baseline warnings unchanged) · links 0 · dupes 0 · doc-check 0 · doc-commands 32/32 · doc-audit 0 · doc-paths 0 · health 0/0 · certify PASS · docgen current · 428 tests OK · built in an ISOLATED worktree off mainline a3d3724 (v1.44.0) · NUMBERING: took 1.46.0/DEC-101/safe-write 0.6.0 and deliberately SKIPPED 1.45.0/DEC-100/safe-write 0.5.0, left free for parallel change line (another session's branch, stale at 1.43.0/DEC-098, must renumber into them) — the gap is cheaper than the race, per the estate's five-collision history · additive; no migration, no folder renames, no registry change, no new schema
- 2026-07-26 · claude (fable) · v1.47.0 runtime-errors census re-extracted from source + diagram atlas catch-up (DEC-102; closes the pass DEC-086 flagged and declined to guess at) · VERIFY-FIRST BRIEF, four items, two were already closed and reporting that was the deliverable: audit finding 4 (per-item Inbox age >14d + generated-file tamper hash) and rider I-2 (`vocab` mandated in the health composite) are BOTH LIVE in source — health.py INBOX_STALE_DAYS/inbox_ages/generated_tamper, health.py:265 composes `vocab`, test_determinism_fixes.py::test_health_composite_includes_vocab — built nothing · THE CENSUS: Appendix D had read 129 messages/19 modules since before okf-*/freshness/upgrade/pack existed; re-extracted by AST walk = 241 rep.error/rep.warn CALL SITES across 31 modules; nine modules had NO ROW AT ALL; validate 27→38, now agreeing one-for-one with the §6.5 rule table v2.12.0 resynced · THE TRAP, RECORDED WITH THE NUMBER: a plain grep returns 235 and is wrong for EXACTLY THREE modules — docaudit/doccheck/docpaths raise through `_sev(rep)` (DEC-083 master=error/instance=warning), invisible to text matching — so the extractor note now says HOW to count; the next pass reproduces the number instead of re-deriving the method · APPENDIX E LEFT ALONE ON PURPOSE: it self-declares as a first-draft/v1.17.0 snapshot, so its `Error messages | 112` is TRUE HISTORY (§4.8), and 'correcting' it would have been the error · GATE-BLINDNESS NAMED NOT CLOSED: doc-audit has no `error messages` truth key (that is why five releases of silence); adding one is a build, deliberately not improvised into a docs release · ATLAS: diagram 08 drew 8 note classes, `working` (DEC-096) missing — added as a 9th box + caption (project material; project layer only; outside default retrieval), canvas 272→312, no existing box moved or narrowed; diagram 14 drew the PRE-TRIAGE flow — the gap the CATALOG carried as a written follow-up marker since v1.38.0 (DEC-093) — Triage → delta register inserted between Extract and Connect + the four outcome families (Knowledge · Action · Governance · Nothing) stated before the nine effects, re-laid out as TWO ROWS OF THREE at 168px rather than squeezing six into one row, so boxes got WIDER not narrower and no label was shortened to fit (text-fit computed against the design system's 12px `ts` metric, both renders visually confirmed before commit), canvas 332→452, CATALOG marker removed · VERIFIED CORRECT AND LEFT ALONE: 31 (fixed at v1.46.0), 41/42/43 checked line-by-line against manage-todo/start-project/record-decision incl. manage-todo's 'one list at the root, per-project is future scope', 44 · constitution 2.12.0→2.12.1 + TWENTY derived docs re-stamped (nineteen was the v2.4.1 figure; `Decisions/_ABOUT.md` arrived since — counted, not copied) · docs and art only: no schema, no migration, no behaviour change · built in an ISOLATED worktree off mainline aab9011 (v1.46.0); 1.45.0/DEC-100 gap left untouched
- 2026-07-26 · claude (fable) · v1.48.0 the OKF surface follows the standard to v0.2 and states the version everywhere (DEC-103) · TRIGGER: Google shipped OKF v0.2 on 2026-07-24 (knowledge-catalog 780fe9d3, #227) and all three commands + ~12 docs were pinned to v0.1 — still TRUE, which is the exact failure mode the standing rule names · SPEC READ FROM SOURCE (1003 lines + the v0.1 text, diffed): supersedes `timestamp`→`generated{by,at}` and body `# Citations`→`sources` (both with sanctioned fallbacks); adds provenance (`sources` entries + credibility signals author/usage_count/last_modified + `usage_window`), trust (`generated`, `verified` as {by,at} EVENTS), lifecycle (`status`, `stale_after`), attestation (`Attested Computation` + runtime/parameters/computation/executor/attester), actor convention §7 · CONFORMANCE UNCHANGED — v0.2 §11 = v0.1 §9, THREE rules; my first read said two because rule 3 fell in a gap between two chunked file reads, and the ADVERSARY PRE-BUILD GATE (DEC-099 step 3b, run by an independent subagent, verdict proceed-with-amendments, 8 findings) caught it against the source before it reached shipped prose — the correction is recorded in lib/okf.py and the result doc rather than quietly fixed · VERSION IS A STATED FACT: --okf-version on okf-check + okf-export (default 0.2), declared in the bundle's root index.md frontmatter (§12), its okf.yaml, its index prose, and the check's verdict; export self-gates via okf-check AGAINST THE VERSION IT CLAIMS; --okf-version 0.1 emits the old shape unchanged · TWO NAME COLLISIONS (`status`, `sources` are now OKF fields too): status mapped + x_aios_status kept, sources rebuilt as OKF entries for IN-BUNDLE records only (a dangling vault id is not a §5.1 scope descriptor) + x_aios_sources kept, review_due→stale_after; a vault's raw folders raise ONE rolled-up advisory, never per-note, never an error (§11 forbids rejecting for an optional family) · WHAT IT REFUSES TO EMIT IS THE POINT: `verified` NEVER emitted (vault holds a verification GRADE, no actor, no date; v0.2 holds EVENTS — synthesising {by,at} fabricates both halves, so every exported concept sits honestly in §5.3's lowest tier), `generated` only with an operator-asserted --author (§7-validated), no Attested Computation ever authored; --verified-by deliberately NOT offered and the asymmetry argued in the module docstring · TWO DEFECTS FOUND AND FIXED NOT FILED: (1) okf-check had NEVER implemented conformance rule 3 since v1.25.0 — reserved files skipped outright while the command printed a three-rule verdict; structural MUSTs now enforced in both versions; (2) the privacy fence covered CONTENT but not REFERENCES — an id is a slug of its record's title, so entities:[person-jane-doe] carried a name out of a bundle whose subject was excluded; all 7 id-list + 2 single-id fields now filtered to records that may themselves travel, withheld count reported; plus unrecognised `status` now fails closed on export (DEC-066 on a branch that had only a blacklist — v0.2's mapping would have laundered `Active`→`stable`) · IMPORT: v0.2 families land verbatim under x_okf_* with x_okf_extra catching everything else (an Attested Computation's whole contract survives), bare `verified` mapping read as a one-element list per §5.2/§11, their stale_after→our review_due, their `verified` NEVER raises our verification · PROOF: Google's own 4 published bundles = 53 concept docs, conformant with ZERO advisories, imported with every family intact; round trip measured ORIGINAL NOTE → LANDED NOTE (the old test compared the emitted bundle to the landed note and structurally could not see a loss introduced on the way out — adversary finding 5); 6 planted privacy markers + all 9 reference fields · HONEST COMPETITIVE READ RECORDED: provenance + verification are NO LONGER differentiators (v0.2 is ahead of us on verification events); what survives is authority, supersedes/contradicts, privacy classification, governed determinism, the kill switch — buyer docs rewritten to say that · DOC SWEEP (grep, not doc-audit — a wrong version string is not a countable claim): README, START-HERE, Product Overview, Human User Manual, How the System Works, Backup and Recovery Options, AI Operator Manual, Flows/Automations Reference, ingest-and-connect SKILL, NEW Glossary entry, Privacy and Boundaries (reference fence), Active Decisions (DEC-103), constitution §6.2/§6.3 rows + §6.4 census 40→42 flags + amendment 2.13.0, 20 derived docs re-stamped · new result doc System/Tests/results/okf-v02-conformance.md; the 2026-07-16 v0.1 record left UNEDITED (dated evidence, protected object) · 472 unit tests OK (428 before) · full gate suite green · baseline regenerated · built in an ISOLATED worktree off mainline aab9011 (v1.46.0) · NUMBERING: 1.48.0/DEC-103/constitution 2.13.0; 1.47.0/DEC-102 belong to queued change; 1.45.0/DEC-100 remain RESERVED and untouched · additive, no migration
- 2026-07-27 · claude (fable) · v1.49.0 certify, health and checkpoint stop lying about what they do (DEC-104; owner authorization 2026-07-27 "fix everything carefully and safely") · FOUR DEFECTS, ONE SHAPE — a command's stated contract and its code disagreed, and the DOCUMENT was what got trusted; every one re-verified against live source before a line changed, on a scratch personal-instance scaffold · (1) certify's PII scan carried the comment "a master-only invariant; personal instances legitimately hold PII" from the day it was written and NEVER READ instance_role — 34 errors on a real instance, every one a legitimate colleague work address, so the product's own certification command was unreachable for the product's own users; now gated via _is_master() on the doccheck/docpaths/docaudit pattern (DEC-083), absent/unreadable role stays STRICT so scaffolds and the master fail closed · (2) certify WAS NOT READ-ONLY: the idempotency probe runs `generate` twice and restored System/Generated only, while generate step 9 baselines into the TRACKED System/Logs/freshness-ledger.json — now a named PROBE_SIDE_EFFECTS set (ledger, event log, freshness registry) preserved and restored, and a file the probe CREATED is deleted again; the generic master never activates freshness, which is exactly why release prep could never see it · THE REPORT NAMED THE WRONG COMMAND AND THAT IS RECORDED: the defect was filed against `health`; health was tested the same way (whole-tree hash before/after) and is GENUINELY read-only with no ledger writer at all — the mutation came from certify, which the same audit ran seconds later; bisected by running all 13 read-documented commands against a git-clean scratch instance and diffing · (3) checkpoint's bare `git add -A` swept three sessions' files under a label naming none of them; DESIGN CHOICE ORDERED BY WHAT MUST NOT BREAK — checkpoint is the kill-switch-exempt safety net, so refusal is NOT the default and the default still commits everything (a checkpoint that can refuse to save is not a safety net); what is gone is the SILENCE: the commit message now ENUMERATES every path it contains + rep.info committed_paths, --paths declares a scope and leaves the rest untouched as left_uncommitted, --strict refuses and explains for automation on a tree it does not own · (4) Metadata Dictionary claimed "validator maintains the reciprocal superseded_by" against a validate.py with ZERO writes — doc corrected to say the link is AUTHORED during a safe-write supersede, and validate deliberately NOT given write behaviour (a validator that repairs what it validates cannot be trusted to report); class sweep over the whole doc set found four more, all fixed: Automations Reference's `approval reset` presented as machinery (nothing in System/Scripts reads `approval` and rewrites it, and nothing DETECTS a missed reset), journal-append + auto-checkpoint overstated as universal when only migrate/import/upgrade/pack perform them, its blanket "you never trigger any of it" header, and Glossary's "promotion sets review_due automatically" against a field only `review-due --set-missing` writes · SEEDED-FAILURE PROOF: new test_read_only.py (16 tests) run against the PRE-FIX code = 8 failures + 1 error, against this code = all pass; new System/Tests/scripts/scaffold.py makes a fixture STATE its instance_role instead of inheriting the host vault's — that inheritance is why 20 doc-gate-severity and upgrade tests failed whenever the suite ran from a personal instance, which is exactly what certify's regression dimension does on a real vault · census flags 42→44 (--paths, --strict); constitution 2.13.0→2.14.0 + 20 derived docs re-stamped · built in an ISOLATED worktree off mainline c6a3772 (v1.48.0, after the preceding reviewed changes); 1.45.0/DEC-100 left untouched · additive, no migration
- 2026-07-27 · claude (fable) · v1.50.0 the doc-truth gates can see what they were blind to, and the censuses are emitted rather than typed (DEC-105; owner authorization 2026-07-27 "fix everything carefully and safely") · GATES FIRST, ON PURPOSE — the brief said the gate upgrades matter more than the text fixes because they stop the class recurring, and they did: nine of the documentation fixes in this release were found by the upgraded gates rather than by reading. Six blind spots closed in doc-audit/validate/health/docgen: word-numbers above twenty were unparseable and silently skipped (the constitution writes every census heading in that notation); strict adjacency hid "9 **AI** playbooks" three times; only *.md was scanned, so the always-loaded Task Router, the atlas metadata and 44 SVGs were invisible; ten classes of countable fact had no truth key (a probe claiming 999 unit tests passed everything); nothing checked behavioural prose, so DEC-101's ruling stayed contradicted in the router for twelve days after eleven docs were swept; and x_reviewed_against failed open on a versionless source (3 dead stamps) while 22 more had been re-applied without a re-read. docgen's own capabilities builder was DEAD — globbing the flat form retired at DEC-070, returning "the 0 capabilities" — and its test asserted "0" appears in "the 0 capabilities", so the test agreed with the bug. Fixed before the README was wired to it. STAMP BACKLOG CLEARED BY READING, NOT RE-STAMPING: all 22 documents opened and checked against the corrected source before updated: moved; that read found a privacy claim in the Claude Adapter Guide, live since v1.0.0-rc.1, that a prior audit had dropped as unreproducible. Seven constitution sections became docgen blocks (§6.8, §8.2, §8.3, §9.1, App D, App E ×2) — §8.5 recommended exactly this on 2026-07-14, was approved, and was never executed. Gates: validate 0/5 · links 0 · dupes 0 · doc-check 0 · doc-commands 0 · doc-audit 0 (claims checked 32→68 — 46 in markdown, 9 in YAML, 13 in SVG, on a doc set whose wrong claims were also DELETED by this release; truth keys 20→30) · doc-paths 0 · okf-check CONFORMANT · docgen 0 · health 0/0 · certify 6/6 PASS · 517 tests OK. NOT DONE, deliberately: GAP-4 (doc-version strings other than system_version are still unaudited — probe F of the audit's experiment is the one that still misses) and GAP-8 (the decision register's blanket doc-paths exemption); DEC-053/054 are a Workshop-register gap, reported to the owner rather than fixed from here.
- 2026-07-27 · claude (fable) · v1.51.0 the shipped product stops naming the owner's employer (DEC-106; owner ruling 2026-07-27 verbatim "Scrub the employer's name from the shipped product") · INVENTORY REBUILT FROM THE TREE, NOT INHERITED: the employer name, the owner's given name and surname, both account handles and /Users/-style absolute paths, case-insensitively, over every tracked file including YAML, Python, SVG, fixtures, Packs/**, .github/ and .obsidian/ = 24 occurrences in 12 files; ZERO in Packs/**; ZERO absolute paths carrying the local username · FIVE LIVE-PROSE SITES REWRITTEN, EVERY ONE KEEPING ITS PROOF (the rule: a provenance claim is evidence the feature was tested on real material, so anonymizing drops the identity and never the evidence): constitution §0.2 out-of-scope list now states the KINDS of instance rather than five names; import_vault.py docstring keeps BOTH file counts (1,095 verbatim / 426 mapped); dupes.py convention-basename docstring now states the case that justifies the rule (a method-mandated per-project HANDOFF.md, two projects, two handoffs, first fully-synced live instance run on v1.30.x) which is strictly more informative than the name was; two test_commands.py docstrings, the F-1 one KEEPING its commit hash 5d7782a because a hash is a pointer to evidence not an identity; fixture 27's filler byline (five-line count and expected.md unchanged) · DELIBERATELY NOT EDITED, AND THAT IS HALF THE DECISION: CHANGELOG, System/Journal, System/Tests/results, System/Migrations and this register's own DEC-050/052/084 keep every name under §4.8 — a dated record is true as of its date and rewriting release history to remove a company from it is the falsification the rule exists to forbid (same reasoning that kept Appendix E intact at v2.12.1); LICENSE/repo URL/pack.yaml source/README attribution are the licensor's own and are correct as-is · ONE HIT REPORTED NOT DECIDED: measure.py's VOICE_PATTERNS carries the owner's given name as a regex alternative — not provenance prose, not the employer, and deleting it changes how a real transcript is measured, so it is left for an owner call (recommended fix if he says remove: make the human-voice alternatives user-extensible from 80 User/ like dupes-conventions, rather than hard-code a second name) · CAUGHT IN SELF-REVIEW: the first draft of the CHANGELOG and register entries QUOTED the strings being removed, reintroducing them into the shipped tree — the describing prose now names no identifier · constitution 2.15.0→2.15.1 + twenty derived docs re-stamped as CONFIRMED UNAFFECTED, verified by searching all twenty rather than assumed (v2.12.1 precedent; the one that still matches is this register, whose hits are its own protected dated entries) · prose and fixture data only: no schema, no migration, no behaviour change, no count moved · built in an ISOLATED worktree off mainline f274ef8 (v1.50.0, after the preceding reviewed changes); 1.45.0/DEC-100 left untouched
- 2026-07-27 · claude · v1.52.0 (DEC-107) — documentation routed by depth · MERGE-AUTHORED RELEASE RECORD for reviewed change (`the documentation-routing change`), which shipped its prose with no version bump, no CHANGELOG entry and no register line · `reading_order.human` becomes three named tiers (Get Started / Understand It / Reference-on-demand) with the Human Onboarding Guide moved up ahead of the Human User Manual; onboarding stage 1 names the tiers instead of reciting a list, in all three parity docs (all stay 1.9.0 — verified not regressed) and states reading is offered, never required · How the System Works §10 gains the "In the field's terms" column + the memory-layer/context-engineering/AI-governance placement with `owned` as the differentiator (1.2.0→1.3.0); Glossary +4 field terms; Product Overview second-brain framing (1.2.1→1.3.0) · TWO CONTRADICTIONS THE BRANCH LEFT BEHIND, fixed here: START-HERE's Reading order ran the superseded sequence and omitted the Human Onboarding Guide entirely, and the constitution stated the old order in §8's dependency-6 row and called the list "flat" in §2.1 · gate lesson recorded — no check went red because the count (6) and the set were right and only the ORDER was stale; `doc-audit` audits counts, not sequence · constitution 2.15.1→2.15.2, twenty stamped docs re-stamped confirmed-unaffected after searching all twenty for sequence prose · baseline regenerated · prose + one manifest key only, no schema/migration/behaviour change · manifest 1.51.0→1.52.0
- 2026-07-27 · claude (fable) · v1.53.0 a stranger's pack manifest is reported, never raised (DEC-108; owner authorization 2026-07-27 "fix everything carefully and safely") · FOUND BY MEASUREMENT, NOT BY A BUG REPORT — reading the gate against the two real pack manifests in the estate showed they disagree about shape: PMM Engine declares a flat top-level `resources:` list, Content Engine nests it under `components:`, and `aios pack` called `.values()` on both, so a PMM-shaped ZIP raised `AttributeError: 'list' object has no attribute 'values'` instead of returning a verdict · BOTH SHAPES MADE CANONICAL because the gate never cared which one it was — its only question about a listed path is whether the ZIP contains it, the grouping keys are documentation, and refusing either would break a shipped pack for no safety gain; `components:`/`resources:` now take a mapping, a flat list or a single string, top-level or nested, and a listed folder is satisfied by anything beneath it (a ZIP need not store directory entries — that alone was rejecting a valid PMM pack) · THE CLASS, NOT THE INSTANCE: eleven crash-instead-of-report cases fixed (invalid YAML, non-UTF-8, manifest-as-list, manifest-as-scalar, corrupt ZIP, encrypted ZIP, depends_on-as-list, path-entry-as-mapping, path-entry-as-number, group-value-as-number, resources-as-string), each with a seeded-failure test asserting an actionable message, a non-zero exit and a tree hashed identical before and after, under --verify and --apply --confirm alike · THREE WORSE THAN CRASHES: a ZIP member escaping the pack folder passed the gate and was caught only at placement, after the installed pack had been deleted and the replacement half-written, with the RuntimeError escaping run() so the DEC-077 recovery never ran (now refused at the gate, before the checkpoint; placement failures now enter that recovery); `aios pack ../../x --remove --confirm` deleted a directory OUTSIDE the vault and reported "leave_no_trace: validate green" (now refused — a remove target is a folder name, never a path); and a duplicate top-level key was silently accepted so a manifest declaring `kind:` twice was gated on the second · CONTRACT IS NOW A FILE: System/Schemas/pack-manifest.yaml, loaded and gated against, in-code constants kept only as a loud fallback with a drift test — closing the gap that let PMM ship `kind: role` unnoticed for a month; it carries no top-level `schema:` key (validate.load_schemas would register a phantom note class) and no `fields:`/`required:` (doc-audit unions those to count note fields), and three census derivations that assumed every file in System/Schemas/ is a note class now read the key instead of the filename · SCOPE HELD: the `layout:` block and scaffold-vs-workspace semantics of SPEC-pack-workspace-split.md deliberately NOT built — owner-gated separate feature; this schema describes only today's contract so that spec stays additive · proven on the real 278-file PMM Engine ZIP (gate PASSED, 0 errors) and the Content Engine shape · unit tests 517→531, test_pack 11→25, call sites 262→270 · full gate suite green
- 2026-07-27 · claude · v1.54.0 (DEC-109) — five gaps between what the machinery claims and what it does; each verified against live source BEFORE any edit, each closed with a seeded-failure test proved to fail against the pre-fix code · **CI push filter** `[main, maintenance branches, docs/**, feature/**]` → catch-all `["**"]` + concurrency group: the active branch set was measured rather than guessed, and the allow-list omitted the prefixes used by most working branches; allow-list rejected as a hand-maintained census of a set the next branch name changes · **`aios upgrade` integrity fingerprint** rebuilt as the COMPLEMENT of the upgrade surface (was `manifest.directories.content` + `.obsidian` + `Packs`): measured on the live work instance at **160 of 1,641 files verified, 1,102 of the gap in seven folders that instance registered in its own folder-registry** — EXTENDED not narrowed because a full-tree fingerprint of that vault costs 0.11s (re-verified read-only: 1,294 files, 0.25s); now also covers the change journal, certification records and root user files, all promised by the docstring and never checked; report states its own scope and names the two regenerated folders it cannot cover · **doc-audit component-version truth key** (GAP-4) — found **10 of the constitution's 17 capability/agent stamps stale** (`safe-write` v0.2.1 vs 0.6.0), all corrected; exempts transitions, amendment logs, register dated figures and dated provenance; PRINTS the three classes it cannot reach rather than implying them · **doc-paths** loses the blanket `Active Decisions.md` exemption (GAP-8) — matches doc-audit's shape (counts dated, paths live); found `System/Capabilities/manage-packs.md` dead since DEC-070 in the entry that introduced it; 168→205 references checked; sentence-scoped removal allowance, count reported · **measure.py** owner's given name → `80 User/transcript-voices.yaml` per owner ruling (configurable, NOT deleted — deleting changes what a real transcript measures); escaped + shape-checked labels, malformed config reported never fatal, every run prints which marker set it used · Claude Adapter Guide privacy claim SETTLED by reading the file: already fixed on main, no change needed · docs: Upgrading step 3b, Human User Manual §8 (+4th hook) and §upgrade, Glossary doc-audit/doc-paths entries, ingest-and-connect step 3a (0.16.0→0.17.0) · constitution 2.15.2→2.16.0 (amendment v2.16.0), 20 stamped docs re-stamped · baseline regenerated · built in an ISOLATED worktree from mainline · manifest 1.53.0→1.54.0
- 2026-07-27 · claude (fable) · v1.55.0 the portability check reads the whole folder, and states how much of it it read (DEC-110) · MEASURED FIRST, ON A LIVE ROLE-PACK INSTANCE: `okf-check` reported `concepts_checked: 9 / CONFORMANT` while **236 notes sat in `Packs/` and were never opened** — the entire client-facing corpus, outside the portability guarantee it was sold under, with nothing in the output saying so · diagnosis corrected from the brief: `Packs/` is invisible to this command because it is not a manifest content directory, NOT because `frontmatter.SKIP_DIRS` prunes it (that is why `validate`/`links`/`dupes` miss packs; `okf-check` never calls `iter_markdown`) · default scope now = manifest content dirs + every installed pack's content, derived as the COMPLEMENT of new `vaultpaths.PACK_MACHINERY_DIRS` (the DEC-108 shape, chosen for consistency with it) so pack content folders are covered by construction; machinery + pack-root files excluded, mirroring `System/` and the vault root · report states its own scope BEFORE the verdict (per-tree coverage, per-pack contribution incl. an explicit `0` for machinery-only packs, concept count inside the verdict sentence) · DEC-061/DEC-067 NOT regressed and structurally cannot be: this command builds no id index, so there is no namespace to merge — guarded by a new test with two packs + the vault all carrying `README.md`/`CHANGELOG.md`/a colliding id (dupes green, canon id space clean, no `Packs/` path in the canon walk) · after: same instance reads 245 checked / 183 conformant / **62 violations, all inside the pack** — pre-existing and previously invisible, reported not hidden · +8 tests (517→525) · built in an isolated worktree off mainline 243c4ea · NUMBERING: 1.53.0/DEC-108 left to the queued change, 1.45.0/DEC-100 untouched · landing-copy sentence drafted for owner approval, NOT shipped
- 2026-07-27 · claude (fable) · v1.56.0 a customized instance can record that it upgraded (DEC-111; owner authorization 2026-07-27 "start fixing all of this carefully and safely so we can actually wrap this all up") · FOUND BY MEASUREMENT ON THREE LIVE INSTANCES, not by a bug report: all three took v1.55.0's `System/` tree cleanly and **none could bump its `system_version`**. Two were correctly blocked by red gates (the personal instance: 25 pre-existing duplicate titles in its own content; the commercial-role instance: a stale composite census in README.md). The third, the green customized work instance, was **green on every gate — validate 0 · links 0 · dupes 0 · health 0, 318 of 322 shipped files byte-identical — and still locked out**, reporting "2 unresolved conflicts in protected paths": a customized Task Router (permitted by DEC-073, which states it is NEVER auto-merged; normal per DEC-083) and the constitution's regenerated docgen censuses (REQUIRED by `docgen --check`, which fails `health` without them). Both are things the product asks the instance to do; accepting either was a permanent lockout with no path through the tool · THE FIX IS A DISTINCTION, NOT A RELAXATION — and it landed in THREE places once the first one proved insufficient: (1) CLASSIFICATION — a file whose instance-vs-base difference lies entirely inside docgen regions was not edited by anybody, so it is now PRISTINE → replace, not a customization; the baseline manifest gained a `derived:` map of generated-body-blanked hashes to make that decidable, absent on pre-v1.56.0 baselines where behaviour is bit-identical to before (test); (2) REGENERATION — upgrade now runs docgen SCOPED TO THE UPGRADE SURFACE right after the copy, because a release's censuses are counted against the RELEASE's tree and are stale the instant they land, which reddened `health` for a reason the run itself caused; scoped, never wholesale, after finding a live instance (the commercial-role instance) carrying docgen blocks in its own user-owned root `CHANGELOG.md` that a blanket run would have rewritten and correctly tripped the integrity check on; (3) THE BUMP GATE — a protected-path conflict now blocks unless ACCOUNTED FOR by one of three explicitly encoded routes: `sanctioned` (product-declared instance-owned, never auto-merged — today the Task Router), `derived` (differs only inside docgen regions), `accepted` (new repeatable `--accept <path>`, the "somebody chose" route that keeps the gate a gate instead of leaving it with no exit) · GUARANTEE KEPT, NOT WEAKENED: nothing is merged, nothing is overwritten, `--accept` keeps the instance's copy exactly as before; each accounted divergence gets its own heading in the durable report AND the manifest's `system_version` line carries the reason on itself, so the version and its caveats cannot be read apart; an `--accept` naming a path not in conflict WARNS rather than being ignored, so a typo cannot read as a resolution; `derived` is deliberately strict because it grants a version claim — a real docgen region required, everything outside the regions byte-identical (one edited sentence disqualifies the whole file), markers compared so adding/removing a block is a real difference, unreadable files never derived · VERIFIED AGAINST ALL THREE REAL VAULT SHAPES on read-only copies, live vaults never written: **the commercial-role instance 1.32.0 → 1.56.0 clean, all gates green, zero manual steps**; **the green customized work instance 1.46.0 → 1.56.0 clean** after the routine post-constitution-bump re-stamp of its own `Decisions/_ABOUT.md` (user-owned, outside the upgrade surface, DEC-069 — the same manual step the v1.55.0 sync recorded); **the personal instance still correctly blocked** by its 25 pre-existing duplicate titles, which is the gate working. A/B control run with the v1.55.0 code on the same three copies: all three refused the bump · ONE HONEST LIMITATION: instances in the field hold pre-v1.56.0 baselines with no `derived:` map, so this one transition needs `--base <v1.55.0 tree>` for exact classification; from v1.56.0 onward the shipped baseline carries it and no flag is needed · +10 tests (570→580), one new flag (44→45) · constitution 2.17.0→2.18.0, 20 stamped docs re-stamped · baseline regenerated · built in an ISOLATED worktree off mainline 53c69f8 · 1.45.0/DEC-100 left untouched, `parallel feature change` never touched
- 2026-07-27 · claude (fable) · v1.57.0 a pack update stops destroying the client's own work (DEC-112; owner authorization 2026-07-27 "start fixing all of this carefully and safely so we can actually wrap this all up") · BUILT FROM SPEC-pack-workspace-split.md, finding F4, open since the Phase 1 extraction and re-confirmed live 2026-07-27 · THE PROBLEM IS NOT A BUG, IT IS A CONTRACT THAT AGED: DEC-061's "install places the folder; update replaces it wholesale; delete removes it leave-no-trace" is CORRECT for a pure vendor scaffold and destructive for a content-bearing role pack, which is now the commercial shape — `Packs/<name>/` holds the client's messaging hub, sales enablement library, intelligence hub, published assets, drafts, voice profile and company config, and an update took all of it while the command reported success; uninstall was worse FRAMED than the update, because "leave-no-trace" was written when a pack held nothing but vendor files and on a role pack it reassures the person about to lose their corpus · `pack.yaml` gains `layout:` with three classes — `scaffold` (vendor: replaced, deleted), `workspace` (user: never read/diffed/replaced/deleted, seeded ONLY where the declared path does not yet exist so the vendor never re-seeds over what the user curated or deleted), `seed` (vendor starter content decided per FILE, written only where absent, the user's once present) — longest declared prefix wins so a vendor folder can be carved out of a user workspace or the reverse; a path declared in two classes is REFUSED rather than guessed, because the wrong reading is destructive · BACKWARD COMPATIBILITY WAS THE ACCEPTANCE BAR AND IS MET BY CONSTRUCTION: an undeclared path is scaffold, so a pack with no `layout:` behaves bit-identically — asserted directly AND by the pre-existing `test_pack.py::test_update_replaces_never_merges`, deliberately left unmodified · but silence must not read as SAFETY, so the shown plan now states which paths are protected and says outright when none are ("this pack declares no workspace; everything under Packs/<id>/ will be replaced") · edited scaffold NEVER auto-merged: install fingerprints the vendor scaffold into `Packs/<name>/.pack-scaffold-baseline.json` (the DEC-074 mechanism scoped to a pack), update keeps the user's copy and writes the incoming one beside it as `<name>.incoming`, reported and warned — DEC-073's core rule applied to packs · UNINSTALL EVACUATES to `90 Archive/packs/<name>-<date>/` preserving relative structure and naming what moved where; the guarantee that MATTERED (no vendor residue, clean vault) is untouched, and only the power to take the user's work is removed · `--purge` is the sole route to the old behaviour: named explicitly, still --confirm-gated, prints the total file count AND how many are the user's before taking them · THE VERSION FLOOR IS THE ENFORCEMENT — a manifest declaring `layout:` must declare min_system_version >= 1.57.0 and is refused otherwise, because an older core cannot see the block and would replace the folder wholesale, and min_system_version is the one thing every core has always enforced · contract lives in `System/Schemas/pack-manifest.yaml` (the DEC-108 schema) so a pack author reads it where they read everything else, not only where the code enforces it · +18 tests in a NEW suite `test_pack_layout.py` (570→588), test_pack.py untouched on purpose · docs: Add-Ons and Packs (Update/Remove rewritten + new scaffold-vs-workspace section), manage-packs 0.3.0→0.4.0 (steps 2, 3 and remove) · one new flag (44→45) · constitution 2.17.0→2.19.0, 20 stamped docs re-stamped · baseline regenerated · full gate suite green · built in an ISOLATED worktree off mainline 53c69f8 · NUMBERING: 1.56.0/DEC-111/constitution 2.18.0 left to the previously queued version-bump change; 1.45.0/DEC-100 untouched
- 2026-07-27 · claude (fable) · v1.58.0 pack content stops being a blind spot, and a citation may cross the pack boundary (DEC-113; owner authorization 2026-07-27 "start fixing all of this carefully and safely so we can actually wrap this all up") · BUILT FROM SPEC-pack-content-validation.md · THE PRUNING WAS NEVER THE DEFECT — `Packs/` stays in `frontmatter.SKIP_DIRS` because a pack ships its own README/CHANGELOG and owns its ids (DEC-061/067); what was wrong is that NOTHING ELSE LOOKED: measured live 446 markdown files / 165 scanned / 260 inside `Packs/` never opened, so a green suite was evidence about the vault AROUND the pack and said so nowhere — the same class as the v2.17.0 okf-check scope gap, answered the same way (read it in its own pass, and state what was read) · what was rotting there, found only by hand: 20 broken wikilinks live for a day, invalid `type`, two invalid `subtype`s, an id/type mismatch, 3 duplicate titles, 34 files with no frontmatter, and A FABRICATED ATTRIBUTED CUSTOMER QUOTE THAT SURVIVED ITS OWN REMEDIATION because that remediation's grep concluded "exactly one file" and the second copy sat in the blind spot · new read-only `aios pack-check`: every `Packs/*/` holding a pack.yaml, each resolved against ITSELF ∪ the vault, nothing merged back; `iter_markdown` gains an optional `skip_dirs` param whose DEFAULT IS UNCHANGED and which nothing else passes · DEC-067 NON-REGRESSION IS THE HARD BAR AND IS GUARDED BY TEST, NOT ASSERTED: two packs + the vault all carrying README.md/CHANGELOG.md and a colliding id stay clean, no `Packs/` path enters the canon walk, canon id space duplicate-free, `iter_markdown` default proven identical — PLUS a guard that fails loudly if anyone later removes `Packs` from the default skip set, which is exactly the shortcut that would regress this silently · severity role-scoped per DEC-083 (master errors / instance warnings, DEC-082 doctrine) with ALWAYS_STRICT never downgraded — parse, id-grammar, duplicate-id, governance-vocabulary, secrets — because those are not matters of taste about a user's own notes; an id shared with the vault is a WARNING never an error (a pack is entitled to its namespace) and missing frontmatter is a warning or nobody runs the check; composed into `health` (so `certify` carries it) with warnings ROLLED UP as one line · TWO FALSE POSITIVES CAUGHT IN SELF-REVIEW BEFORE THEY SHIPPED: the type set was the note-class schemas alone, flagging legitimate `type: capability`/`system-doc` — now reuses validate's OWN `known_types` so a pack is never judged by a narrower rule than the vault it installs into; and the secret scanner matched the product's own documented placeholders until FAKE/EXAMPLE/PLACEHOLDER markers were excluded · IT FOUND A REAL DEFECT ON ITS FIRST RUN in this repo's own shipped pack: `obsidian-bases` vendors kepano's SKILL.md linking to `references/FUNCTIONS_REFERENCE.md`, a path our packaging had FLATTENED — both links dead since the pack shipped; fixed by restoring the layout the vendored file expects (git mv into `references/`, pack.yaml + capability path updated) rather than editing third-party content, so the vendored file stays byte-identical · CROSS-BOUNDARY CITATION takes the spec's option (a): pack ids are a SECONDARY index consulted only by the link resolver, so a vault note declaring `sources:` into a pack no longer errors — resolution is not membership, pack ids never join the canon id space, and cross-boundary refs are REPORTED so the dependency is visible; §6.3 hardening: `aios pack --remove` REFUSES when a vault note cites an id ONLY that pack defines and names the citing notes (an id the vault or another pack also defines still resolves and is not a reason to refuse) · TWO SEPARATE ITEMS HANDLED SEPARATELY, as the spec asked: `links.py:42` held filenames WITH their extension while the lookup used raw link text, so `[[Guide]]` and `[[Guide.md]]` returned opposite verdicts and the passing spelling bypassed the id/title check entirely — its own fix, its own test; and `test_pack.py`'s comment claimed its fixture fails on a duplicate id, which DEC-067 makes IMPOSSIBLE (Packs/ is pruned, validate never sees the pack's copy) — it fails on the id/type-prefix rule, comment corrected, test and subject unchanged · +22 tests in a new suite `test_pack_check.py` (570→592) · one new command (31→32), no new flags, no schema change · docs: Add-Ons and Packs (two new sections) · constitution 2.17.0→2.20.0, 20 stamped docs re-stamped · baseline regenerated · full gate suite green · built in an ISOLATED worktree off mainline 53c69f8 · NUMBERING: 1.56.0/DEC-111 and 1.57.0/DEC-112 left to the two previously queued changes; 1.45.0/DEC-100 untouched
- 2026-07-27 · claude (fable) · v1.59.0 following the documented path is not a duplicate (DEC-114) · DIAGNOSIS VERIFIED AGAINST LIVE SOURCE BEFORE BUILDING, and it came back different from the brief: the historical evaluation instance has **26 title-collision groups, not 25** — one is the DEC-084 `handoff` convention group, already a warning, so the 25 ERRORS are 24 cited `source`→`note` derivation pairs plus **one that is not a derivation at all** · THE OUTLIER DECIDED THE DESIGN: a personal to-do record vs an unrelated work-capture record — distinct types, distinct folders, NEITHER citing the other, and reading both files shows genuinely unrelated content · so THE CITATION IS REQUIRED for the downgrade: a rule keyed on differing `type` alone would have cleared all 25 and silenced a real collision to improve a number · downgrade = WARNING not silence (DEC-084 precedent: legitimate-by-design collisions stay visible, never red) · rule: every record typed + ids distinct + `type` distinct + folders distinct + the group CONNECTED by its own `sources`/`derived_from` edges (the two lineage relations in the Metadata and Relationship Dictionary; `related`/`depends_on` earn nothing) · connectivity lets source→note→output pass as one lineage while an uncited third party drags the group back to an error · TEST-VAULT-FIRST on a fresh synthetic install copy: defect reproduced red, then 8 behaviors proved green/red as specified before the product tree was touched · 4 new unit tests (620→624), one using subTest to probe same-type / same-folder / duplicate-id / `related`-is-not-lineage individually · fixture 31-derivation-titles · PAYOFF VERIFIED ON A READ-ONLY COPY of the live instance (copied, never run against the live vault): **25 errors → 1**, the To-Do collision, nothing newly red · NOTHING WAS WRITTEN TO THE SOURCE INSTANCE · built in an isolated worktree off mainline 53c69f8 · NUMBERING: three queued release changes retained their assigned versions and decisions; 1.45.0/DEC-100 untouched · constitution 2.20.0→2.21.0 · baseline regenerated · full gate suite green
- 2026-07-28 · claude (fable) · v1.60.0 the machinery stops failing on things that are not the user's work (DEC-115; owner authorization 2026-07-28 "let's wrap this all up safely and carefully") · TWO DEFECTS OF ONE SHAPE, both blocking a version bump over something no human authored, both diagnosed against LIVE SOURCE before a line was changed · (1) EDITOR OPEN = NO UPGRADE, EVER: `aios upgrade` runs `generate`, `generate` writes index files into the vault, and Obsidian — running and watching the folder — re-saves its own `.obsidian/workspace.json` INSIDE the same run; the DEC-109 integrity fingerprint compared before/after, saw it move, and reported `CONTENT INTEGRITY FAILURE — 1 user file(s) changed`, refusing the bump as "gates red" · measured on all three live instances 2026-07-27: three received the v1.59.0 System/ tree, NONE recorded the version, same single cause; no script in `System/Scripts/` writes the file (only `import_vault.py`, which deliberately SKIPS it, v1.0.4 lesson: session state, not content); the file was stable for 36 s while the vault sat idle and changed across a bare `aios generate` · THE FIX IS A SCOPE CORRECTION, NOT A RELAXATION: `.obsidian/` is runtime application state this command has been declared never to touch since v1.0.4, so it was already outside the upgrade SURFACE and including it in the VERDICT was the inconsistency; new `RUNTIME_STATE_PATHS` names exactly three files — `workspace.json`, `workspace-mobile.json`, `workspaces.json`, i.e. which panes are open — and excludes them from the verdict only · GUARANTEE UNWEAKENED AND PINNED IN BOTH DIRECTIONS: every other `.obsidian/` file is user CONFIGURATION (`app.json`, `hotkeys.json`, appearance, plugin settings) and is still verified byte for byte (seeded-failure test on `hotkeys.json`); a real NOTE changing mid-run is still a hard failure naming the file and pointing at the checkpoint (seeded-failure test on `20 Knowledge/my-note.md`); a session-state file that does move is REPORTED by name in its own `editor_session_state` field with the reason, counted separately in the headline claim rather than folded in with the regenerated skill projections, and `_verify_scope()` states what the check now declines to judge — excluded is not hidden · (2) `pack-check` ERRORED ON THE PRODUCT'S OWN TEMPLATES (introduced v1.58.0, role-gated to `generic-master`, which is why it surfaced on the commercial-role instance and nowhere else): 31 errors blocking that instance's bump — 19 `duplicate id 'note-<slug>'`/`'source-<slug>'` across a pack's 22 templates, when `System/Templates/` ships the IDENTICAL convention including `procedure-<slug>` TWICE and `aios dupes` is green on it; `validate` has exempted template placeholders since the id rules were written and `pack-check` re-derived the rules without it · FIXED THE CHECK, NOT THE CONTENT (rewriting the owner's pack templates to satisfy a wrong check is what fixture 31 forbids one layer down): the rule now has ONE definition — `frontmatter.is_placeholder` + `in_template_space` — called by both readers, recognising the two things the product already knew, the `<…>` grammar (an angle bracket is illegal in ID_RE, so a value carrying one is unambiguously an unfilled slot) and the `Templates` folder (factored out of `validate` UNCHANGED as a substring test, so nothing exempt yesterday becomes an error today) · NOT A BLIND SPOT — THE CHECK GOT STRICTER WHERE IT MATTERS: a placeholder id OUTSIDE a Templates folder is now an error `pack-check` did not make at all before (a shipped record nobody finished); real duplicate ids and real type-prefix violations untouched, each pinned by its own test; a placeholder never enters `pack_id_index`, so nothing can cite `note-<slug>` and be told it resolved · the remaining 12 errors were `unresolved wikilink [[]]`, a FALSE DESCRIPTION of a fill-in slot: `[[ ]]` and `[[<topic>]]` name nothing, there is no note they could resolve to and nothing is broken, and the frontmatter relation fields have skipped `<…>` for this exact reason since the command shipped — the body now agrees with the frontmatter; reported as the empty slots they are, advisory on every role, while a link promising a note that does NOT exist stays an error on `generic-master` (pinned by test) · VERIFIED READ-ONLY ON THE LIVE INSTANCE that produced the 31: the commercial-role instance 31 errors → 0 errors / 22 warnings, 272 files across two packs, nothing written to it · +14 tests (624→638) · no new commands, flags, schemas, components or fixtures · docs: Upgrading step 3b ("You can leave Obsidian open") + scope sentence, Add-Ons and Packs (templates read as templates) · CONSTITUTION DELIBERATELY LEFT AT 2.21.0: no normative section changes, and bumping it would have required re-stamping 20 derived docs against identical text, which DEC-105 names as the failure the stamp exists to prevent — stated in the CHANGELOG numbering note so the owner can add an amendment entry on top if he wants one · baseline regenerated · built in an ISOLATED worktree off mainline f2149b7 · 1.45.0/DEC-100 untouched, `parallel feature change` never touched
- 2026-08-14 · Codex + independent auditors · system/HIGH · v1.64.2 public-release finalization (Workshop DEC-124/125/126) · OWNER AUTHORITY: “approved this version” and “execute everything in parallel, carefully and correctly, no loose ends” approves the plain-language six-stage architecture and Wave 1 public copy/graphics for qualification; Wave 4 public push/release/visibility remains separately gated · CHANGED: reconciled root/onboarding/operator/public truth; added reviewed Public Release allow-list (12 logical imports/16 physical imports plus release-control records); preserved v1.64.1 evidence as historical only; added current plain-language full and concise v1.64.2 architecture SVG/PNG companions; corrected docgen user-note count to 8; set manifest/pyproject/changelog to 1.64.2; retargeted the exact direct historical migration estate to 1.64.2 and added 022 for exact v1.64.1→v1.64.2 · SAFETY: no core transaction rewrite, no runtime certification claim, no public push/tag/release/visibility change, scoped checkpoint `2026-08-14-020143-before-Wave-1-public-system-documentatio.zip` SHA-256 d5b044…; target-bound v1.64.2 evidence remains external to the tagged tree · W1B: owner visual/copy approval recorded; independent content audit found zero factual blockers; Claude different-engine frozen-diff audit, final baseline, upgrade rehearsal, strict source gate and definitive qualification remain required before publication · PUBLIC-DISTRIBUTION PRIVACY: owner authorized a separate clean public repository while keeping development history private; incidental personal, employer, and private-construction identifiers were replaced with stable role labels without changing any result, count, hash, or conclusion; Method Kit paths now resolve inside the shipped tree · QUALIFICATION TOPOLOGY: target and historical authority repositories are separately pinned and mutation-checked, and emitted evidence omits private paths.
