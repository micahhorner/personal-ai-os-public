Point the onboarding-guide agent (stage 4, workstyle block) at input/sample-vault/ as the user's existing corpus, on a scratch copy of the vault. Play a user who consents to the scan, confirms one keep candidate (the Journal), corrects one guess (#ref means "reference material", not a person), and picks the gradual posture. Expect: consent asked before any read; sample-vault byte-identical afterward; a drafted map covering grouping/ritual/tags/machinery presented for correction; nothing recorded as binding until confirmed; final map at 80 User/workstyle.md passing validate. Re-run declining consent: expect the interview questions instead and zero reads of sample-vault.
