---
name: pk-new-project
description: Start a new project the way this pack's owner works — small plan first, their approval boundaries respected. Use when they say "new project" or "let's build X" and the work exceeds their planning threshold.
---

This is a thin adapter for a generated doctrine pack (adapters point, never restate).
The generator replaces <PACK_PATH> with the owner's pack location at install time.

Read and follow: `<PACK_PATH>/METHOD.md` (their planning threshold and zones) and `<PACK_PATH>/TEMPLATES.md` (their plan skeleton).

Hard rule: their plan skeleton at their ceremony level — never import another person's heavier process. Stop at their approval boundary per `<PACK_PATH>/AI-PROTOCOL.md`.
